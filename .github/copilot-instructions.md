# AI Trading Brain — Copilot Instructions

## Core Principle: Intentional Evolution

**Modify existing files only when the change improves architecture.**
**Avoid rewrites that break interfaces.**
**Evolution is intentional, not accidental.**

Before touching any file, answer these three questions:
1. Does this change improve correctness, performance, or architecture?
2. Does it preserve all existing public interfaces (class names, method signatures, return types)?
3. Is it the smallest change that achieves the goal?

If any answer is "no" — don't make the change.

**For any change to a core or protected module, state the architectural impact
before writing a single line of code.** Describe: which layers are affected,
which interfaces are touched, and what breaks if the change is wrong.
This prevents blind modifications and gives the user a chance to redirect.

---

## Change Policy

| Type | Allowed? | Rule |
|---|---|---|
| Bug fix | ✅ | Preserve interface, fix behaviour |
| Performance improvement | ✅ | Same interface, faster internals |
| New feature (additive) | ✅ | Add new methods/classes, don't remove old |
| Refactor for clarity | ⚠️ | Only if it removes a real coupling problem |
| Rename / move | ❌ | Never — breaks imports across 17 layers |
| Rewrite working module | ❌ | Never without explicit user instruction |
| Add new file | ✅ | Preferred over modifying existing wiring |

---

## Architecture Overview

**Full detail:** see [ARCHITECTURE.md](../ARCHITECTURE.md)

**Project:** `C:\Users\UCIC\OneDrive\Desktop\ai_trading_brain\`
**Stack:** Python 3.14 | `.venv/` | 17-layer hierarchical multi-agent system | ~62 agents
**Broker:** Dhan (login ✅, data API blocked 451) → yfinance auto-fallback
**Mode:** Live trading (2026-09-14 — deliberate operator decision; `PAPER_TRADING=false`, `LIVE_TRADING_AUTHORIZED=true`). `main.py --paper` | Telegram bot (`main.py --telegram`)

### Layer Order (do not reorder)
```
1  GlobalIntelligence      — overnight global context (S&P, Nikkei, bonds, FX)
2  MarketIntelligence      — NIFTY/BANKNIFTY regime, sector, liquidity, events
3  MetaLearning            — k-NN strategy weight predictor
4  OpportunityEngine       — equity scanner, options opportunities, arbitrage
5  StrategyLab             — MetaStrategyController, backtesting, evolution
6  CapitalRiskEngine       — position sizing per strategy budget
7  RiskControl             — RiskManagerAI, PortfolioAllocation, StressTest
8  MarketSimulation        — Monte Carlo, 14 scenarios
9  RiskGuardian            — final kill-switch (VIX>45, daily loss>2%)
10 DebateAndDecision       — 5-agent debate, DecisionEngine (threshold 6.5)
11 ExecutionEngine         — OrderManager → ZerodhaBroker (sim mode)
12 TradeMonitoring         — TradeMonitor, StrategyHealthMonitor
13 LearningSystem          — LearningEngine, StrategyPerformanceTracker
14 PerformanceAnalytics    — DrawdownAnalyzer, WalkForwardTester
15 ResearchLab             — promotion gates: WinRate≥50%, Sharpe>0.8, MaxDD<15%
16 ValidationEngine        — 6-stage: Backtest→WFT→CrossMarket→MC→Sensitivity→Regime
17 ControlTower            — SQLite telemetry, Streamlit dashboard, EventBus
```

### Key Singletons — never instantiate twice
```python
get_performance_tracker()    # learning_system.strategy_performance_tracker
get_regime_strategy_map()    # meta_learning.regime_strategy_map
get_telegram_bot()           # notifications.telegram_bot
get_feed_manager()           # data_feeds.data_feed_manager
```

### Critical Interfaces — never change signatures
```python
# GlobalDataAI
GlobalDataAI.fetch(force: bool = False) -> GlobalSnapshot

# SystemMonitor
SystemMonitor.time_layer(layer_name: str) -> contextmanager

# MasterOrchestrator
MasterOrchestrator.run_full_cycle() -> None
MasterOrchestrator.start_scheduler() -> None

# BaseFeed
BaseFeed.get_quote(symbol: str) -> Optional[TickerQuote]
BaseFeed.get_multiple_quotes(symbols: List[str]) -> Dict[str, TickerQuote]
BaseFeed.get_history(symbol, days, interval) -> List[PriceBar]
```

### Latency Thresholds (system_monitor/system_monitor.py)
```python
LAYER_LATENCY_WARN_MS  = 2_000   # per-layer default
LAYER_LATENCY_CRIT_MS  = 5_000   # per-layer default; abort cycle if exceeded
LAYER_LATENCY_WARN_OVERRIDES = {"GlobalIntelligence": 5_000}
LAYER_LATENCY_CRIT_OVERRIDES = {"GlobalIntelligence": 12_000}
```

### Performance Baseline (current, do not regress)
```
GlobalIntelligence    17ms  ✅  (cache + background pre-warm)
MarketIntelligence    19ms  ✅
Full cycle:          172ms  ✅  HEALTHY
```

---

## Files Modified — Running Log

| File | Reason | Interfaces changed? |
|---|---|---|
| `global_intelligence/global_data_ai.py` | 5-min cache + background pre-warm thread | No — `fetch()` sig unchanged |
| `data_feeds/yahoo_feed.py` | `timeout=8` on `yf.download()` | No |
| `system_monitor/system_monitor.py` | Per-layer WARN/CRIT overrides | No |
| `orchestrator/master_orchestrator.py` | +MarketMonitor, +StrategyPerformanceTracker, +RegimeStrategyMap | No |
| `config.py` | SCHEDULE expanded, CONTINUOUS_SCAN_INTERVAL=30 | No |
| `market_intelligence/market_monitor.py` | NEW — 30s continuous scan + 6 deep-scan slots | N/A |
| `learning_system/strategy_performance_tracker.py` | NEW — win rate / auto-disable | N/A |
| `meta_learning/regime_strategy_map.py` | NEW — regime→strategy learning map | N/A |
| `notifications/telegram_bot.py` | +/perf, +/learn commands (13 total) | No |
| `orchestrator/master_orchestrator.py` | Full scheduler rewrite: pre-market init, market-hours guard, all 10 slots | No |
| `main.py` | SIGTERM handler for clean scheduler shutdown | No |
| `scripts/autostart.bat` | NEW — Windows Task Scheduler entry point | N/A |
| `scripts/setup_windows_task.py` | NEW — registers 08:00 weekday Task Scheduler job | N/A |
| `strategy_lab/strategy_generator_ai.py` | Bug fix: `_best_evolved_variant` now filters by `min_signal_rr`; `_load_evolved_strategies` now honours explicit `min_rr` from JSON | No |
| `execution_engine/order_manager.py` | Explicit `PAPER_TRADING` check; persistent CSV journal at `data/paper_trades.csv` | No |
| `main.py` | Dynamic date (was hardcoded); removed shadowing `_dt` local import | No |
| `orchestrator/master_orchestrator.py` | `_do_monitor`: index symbols (NIFTY/BANKNIFTY) exempt from `.NS` suffix — use bare names so `GLOBAL_SYMBOL_MAP` routes to `^NSEI`/`^NSEBANK`; `_do_eod_learning`: recovers CSV-closed trades from today to handle post-restart zero-count; strategy attribute lookup fixed (`strategy` over `strategy_name`) | No |
| `autonomous_research/rc_models.py` | NEW — Pure data models for ResearchCoordinator: `ResearchStage`, `ResearchRun`, `ResearchTelemetry`, `ResearchSummary`, `RCStatus`, `ResearchHealth`, `ResearchStageState`, stage constants | N/A |
| `autonomous_research/rc_config.py` | NEW — `RCConfig` dataclass with per-stage toggles and `dry_run` | N/A |
| `control_tower/telemetry_logger.py` | **DTA-BLOCKERS-001**: SQLite `timeout=30→1` + `PRAGMA busy_timeout=800` on BOTH `sqlite3.connect()` calls in `_connect()`. Prevents 30s×2=60s block per cycle when dashboard reader holds WAL lock on `control_tower.db`. | No |
| `control_tower/mi_latency_audit.py` | **DTA-BLOCKERS-001**: Added `timeout=1`, `PRAGMA busy_timeout=800`, `PRAGMA integrity_check` (corrupt→auto-recreate) in `_get_conn()`. Wrapped `_write_record()` in `try/except sqlite3.DatabaseError` with `self._conn=None` reset. | No |
| `scripts/dhan_auth/dhan_token_agent.py` | **DTA-BLOCKERS-001**: TOTP generated fresh per retry window instead of once; on `Invalid TOTP` (API_ERROR_IN_200) waits for next 30s TOTP window and retries up to 2×. On exhaustion: sends Telegram alert with step-by-step TOTP re-registration instructions pointing to Dhan portal. | No |
| `autonomous_research/__init__.py` | Added RC exports: `ResearchCoordinator`, `RCConfig`, all RC models and constants | No |
| `test_rc.py` | NEW — 190/190 tests (T001–T190) | N/A |
| `models/trade_signal.py` | MOP-RC-001: added `expected_move_pct`, `_obs_candidate_score`, `_obs_regime` (all `Optional`, default `None`) | No |
| `opportunity_engine/equity_scanner_ai.py` | MOP-RC-001: compute+attach observational fields in `scan()`, extend `[EdgeTelemetry]`, call observer | No |
| `opportunity_engine/mop_rc001_observer.py` | NEW — MOP-RC-001 append-only JSONL observer, `record_signal_observation()`, `write_daily_summary_md()` | N/A |
| `test_mop_rc001.py` | NEW — 15 tests (T001–T015): formula, None-safety, invariance, no look-ahead, dedup | N/A |
| `.gitignore` + `.dockerignore` | Gitignore ALL runtime/research data: KEL, shadow evidence, KSL state, options, iv_history, LOL, scanner_memory, regime_history, scheduler_health, trade_analytics, **strategy_performance, odm_state, knowledge_pipeline_health, ml_performance_dataset, paper_trading_daily, discovered_edges, evolved_strategies, ars_hypothesis_registry, ars_study_\*.json, ars/rc/history.json, mover_discovery\*, klp/knowledge_fusion/source_inventory** | No |
| `scripts/safe_pull.sh` | NEW — backs up all runtime data, runs git pull, restores files. Use instead of bare `git pull` in deployment commands | N/A |
| `build_manifest.json` | Regenerated; deploy command now includes `generate_build_manifest.py` to prevent DeploymentDrift | No |
| `orchestrator/master_orchestrator.py` | Universe rebuild: Monday-only guard removed, schedule moved from 08:30 to 16:15 IST, runs daily post-market | No |
| `audit/dta041_pit_discovery_evidence.py` | DTA-041 Phase 2: `record_outcome` for immutable PIT_OUTCOME appends | No |
| `opportunity_engine/klp_outcome_engine.py` | DTA-041 Phase 2: `fill_pending_pit_outcomes` to resolve matured PIT outcomes from OHLCV bars | No |
| `opportunity_engine/historical_behaviour_engine.py` | DTA-041 Phase 2: `_load_pit_discovery_file` + maturity-enforced ingestion into HBE evidence pool | No |
| `tests/test_dta041_phase2_delayed_outcome.py` | DTA-041 Phase 2: 4 new tests proving maturity enforcement, lineage preservation, HBE ingestion, and empirical profile updates | N/A |
| `risk_control/capital_risk_engine.py` | DTA-CRE-DIAG-001: `get_last_cycle_dominant_rejection_reason()` getter exposing the already-computed dominant rejection reason | No |
| `execution_engine/order_manager.py` | DTA-AET-DIAG-001: AET CONFIRMATION deferral log now states the real trigger (distortion flags, or VIX) instead of always printing a VIX comparison | No |
| `control_tower/pipeline_forensic_reporter.py` | DTA-KDA-DIAG-001: `record_kda_authority()` + new `[PipelineKDA]` daily summary line (StrategyLab-override rate as a standing metric) | No |
| `orchestrator/master_orchestrator.py` | DTA-CRE-DIAG-001/DTA-AET-DIAG-001/DTA-KDA-DIAG-001 wiring; DTA-KDA-FINAL-AUTHORITY-001: formalized KDA as sole final BUY/SELL/REJECT authority, StrategyLab explicitly observation-only; DTA-KDA-CONV-002: made the KDA conviction win-rate term symmetric (previously only rewarded win-rate >55%, never penalized <55%, letting large sample size alone produce high conviction on poor-track-record setups) | No |
| `ARCHITECTURE.md` | Layer 5: added explicit ARCHITECTURAL RULE callout formalizing KDA as sole final trading authority, StrategyLab as observation-only | N/A |
| `orchestrator/master_orchestrator.py` | DTA-OPTIONS-CRE-ISOLATION-001 (Phase D root-cause fix): options/spread signals now split out of `enriched_signals` BEFORE `CapitalRiskEngine`, not after `RiskControl` as before. Root cause: CRE ranked equity+options+arb together under one shared `MAX_POSITIONS` cap; equity's 25-40 candidates/cycle vs options' ~2/cycle meant options were crowded out on effectively every cycle (100% of CRE rejections in the `rejection_audit.db` retention window were `MAX_POSITIONS_CAP`). Options now bypass CapitalRiskEngine entirely — `OptionsRiskEngine` remains their sole, independent capital gate. Equity's `enriched_signals`, ranking, and cap are untouched | No |
| `strategy_lab/strategy_generator_ai.py` | DTA-OPTIONS-ISOLATION-001: added missing `Bear_Put_Spread` entry to `STRATEGY_PARAMS` (was absent, causing StrategyLab to silently relabel Bear_Put_Spread signals to `Hedging_Model` via the regime fallback path); exempted OPTIONS/SPREAD signal types from the SHM/PerfTracker active-set exclusion in `_assign()` and the final hard gate in `assign_strategy()` so StrategyLab stays observation-only for options (mirrors the KDA-supremacy rule already established for equity) — equity behavior unchanged | No |
| `strategy_lab/meta_strategy_controller.py` | DTA-OPTIONS-ISOLATION-001: added `Bear_Put_Spread` to `_REGIME_MAP[BEAR_MARKET]` (was missing, so it never appeared in the regime candidate set) | No |
| `risk_control/capital_risk_engine.py` | DTA-OPTIONS-ISOLATION-001: added `Bear_Put_Spread` to `_STRATEGY_SHARE` (12%, mirrors `Bull_Call_Spread`) | No |
| `strategy_lab/backtesting_ai.py` | DTA-OPTIONS-ISOLATION-001: added `Bear_Put_Spread` to `_BACKTEST_CACHE` (mirrors `Bull_Call_Spread`'s pre-seeded gate-passing profile), removing reliance on `filter_by_backtest()`'s fail-open default | No |
| `opportunity_engine/options_opportunity_ai.py` | DTA-HEDGING-MODEL-001: implemented `_build_hedging_model()` — a Bear Call credit spread (defined max loss by construction), realizing the long-reserved but never-built `Hedging_Model` strategy slot (best pre-seeded Sharpe/drawdown in the roster). Registered in the `builders` dict; `_select_strategy()`'s BEAR_MARKET branch now prefers `Hedging_Model` when IVR ≥ `IVR_SELL_THRESHOLD` (enough premium to justify selling), falling back to the existing `Bear_Put_Spread` debit spread otherwise. Chosen over a naive protective put per prior (disconnected) research in `analysis/options_backtester.py` showing protective puts underperform (13.4% WR, PF 0.77) vs credit spreads | No |
| `opportunity_engine/options_opportunity_ai.py` | DTA-OPTIONS-SELF-LEARNING-001 (Phase D completion): `_base_confidence()` now also consumes `OptionsUnderlyingResponseTracker.get_distribution()` (block 3) — the "underlying move → option move" leverage-capture research, previously computed and persisted but never fed into any decision (only debug-logged). Bounded ±0.3 delta, zero effect until the tracker's own `MIN_OBS_FOR_DISTRIBUTION` gate is met with real data. Closes the last unwired link in the options self-learning loop: real trade closes → `OptionsKnowledgeStore`/`OptionsPerformanceTracker`/`OptionsUnderlyingResponseTracker` (all auto-updating, no human gate) → automatically consumed back into `_base_confidence()` on the next signal. Equity untouched — this function is only ever called from the options builders | No |
| `knowledge_system/equity_hedge_shadow_engine.py` | NEW — DTA-EQUITY-HEDGE-SHADOW-001: read-only shadow observer of equity's own `ORDER_PLACED` events (filters `source_agent=="OrderManager"` only — never touches or reads back into equity decisions). For each real equity trade, attempts a single-stock option chain lookup (honestly records `chain_available=False` today — confirmed empirically that no live/synthetic single-stock chain support exists yet) and, when available, shadow-tracks a hypothetical protective hedge (PUT vs long equity, CALL vs short) with zero real capital. Implements the automated `EQH-Ready-1..5` readiness gate (volume ≥40, OOS sign-test p<0.10, profitable net of a 2% cost estimate, no symbol >80% of evidence, single-stock-only evidence) — reuses existing project thresholds, no new numbers invented. When all 5 pass for a symbol, marks `READY_FOR_LIVE` and fires a Telegram alert; does **not** place any real trade — no single-stock options execution path exists anywhere in this codebase yet, so "automatic" here means the AUTHENTICATION decision only, not a live-trading switch | No |
| `orchestrator/master_orchestrator.py` | DTA-EQUITY-HEDGE-SHADOW-001: wired `EquityHedgeShadowEngine` in the same additive, try/except-wrapped spot as the existing OIOS execution bridge (`subscribe(self.bus)` + `start()` background loop) — pure additive EventBus subscriber, zero equity code touched | No |
| `data_feeds/dhan_fno_security_map.py` | DTA-EQUITY-HEDGE-EXEC-001: fixed a real, pre-existing bug in `_download_to()` — `dhanhq` SDK's `fetch_security_list()` now returns a pandas `DataFrame`, not a list of dicts; `if result:` raised "ambiguous truth value" and silently fell back to a stale local CSV every day. Now handles both return shapes explicitly. Added `get_lot_size(underlying)` — reads the real, verified `SEM_LOT_UNITS` field from Dhan's own instrument master (never hard-coded); returns `None` (fail-closed) for unknown symbols | No |
| `data_feeds/dhan_feed.py` | DTA-EQUITY-HEDGE-EXEC-001: `get_options_chain()` now supports single-stock (`NSE_EQ`) underlyings in addition to index (`IDX_I`) — empirically verified against live Dhan API 2026-09-12 (RELIANCE returns real strikes/security_ids/Greeks via `NSE_EQ`, identical response schema to indices). `under_exchange_segment` is now derived from the looked-up symbol's segment instead of being hard-coded to `"IDX_I"` everywhere. Index behavior completely unchanged (same segment value used) | No |
| `execution_engine/options_order_manager.py` | DTA-EQUITY-HEDGE-EXEC-001: fixed a dangerous silent fallback — `lot_size = NSE_LOT_SIZES.get(signal.symbol, 75)` defaulted to 75 (NIFTY-ish) for ANY unrecognized symbol, which would have sized a real single-stock order with the wrong quantity. Now checks `NSE_LOT_SIZES` (indices) then `DhanFnOSecurityMap.get_lot_size()` (verified real instrument-master lot size for stocks); if neither resolves, the order is rejected rather than guessed | No |
| `risk_control/options_risk_engine.py` | DTA-EQUITY-HEDGE-EXEC-001: same fail-closed fix as above — `lot_size = int(meta.get("lot_size", 75))` removed; missing/invalid `lot_size` in signal metadata now rejects the trade instead of silently sizing risk against a guessed value (defense-in-depth alongside the OptionsOrderManager fix, since this method runs first in the pipeline) | No |
| `knowledge_system/equity_hedge_shadow_engine.py` | DTA-EQUITY-HEDGE-EXEC-001 (supersedes the shadow-only note above): live execution built. Structure corrected per user clarification to a SAME-DIRECTION defined-risk companion option (long CALL alongside a long equity trade, long PUT alongside a short one) — not a traditional opposite-direction protective hedge; captures the "underlying move → option move" leverage research with max loss capped at the premium paid. New `_attempt_live_execution()`: only ever runs for a symbol already in `_ready_symbols` (defense-in-depth re-check even if called directly); fetches the real chain, re-validates chain quality/DTE/OI/premium against the same thresholds the index options fast-path already uses, resolves a VERIFIED lot size (rejects rather than guesses), then routes the signal through the existing, already-proven `OptionsRiskEngine.approve_and_size()` → `OptionsOrderManager.execute()` — no new, untested execution path. **Production is configured for live real-money trading** (`PAPER_TRADING=false`, `LIVE_TRADING_AUTHORIZED=true` — confirmed on both local and VPS `.env`, not a test artifact), so this can place real orders once a symbol authenticates; validated exclusively via a fully-mocked test (real chain fetch, but `OptionsRiskEngine`/`OptionsOrderManager` replaced with mocks) to guarantee zero capital risk during verification — real order placement itself remains untested against a live broker fill | No |
| `scripts/knowledge_system/ranking_hypothesis_validator_001.py` | RSL-001 (Ranking Self-Learning Loop, Phase B completion): NEW — automated statistical validator for KSL-001's auto-registered `PROPOSED` hypotheses. Time-ordered 70/30 train/OOS split (never shuffled), bootstrap CI (1000 iters) on the OOS effect, 4-check scorecard (sample size, CI excludes baseline, train/OOS sign consistency, effect magnitude). Verdict ≥3/4→VALIDATED, ≤1/4→NO_INCREMENTAL_VALUE, else→INSUFFICIENT_SAMPLE (retried next cycle). Drives the ARS hypothesis through the same fully-automated lifecycle chain already proven by `h001.py`'s `H-CRITICAL-001` (PROPOSED→...→RUNNING→VALIDATED/REJECTED, actor=`RHV-001`, zero human step). On VALIDATED, hands off to the adjustment engine below | N/A |
| `scripts/knowledge_system/ranking_adjustment_engine_001.py` | RSL-001 (Phase E): NEW — isolated ranking-adjustment engine, deliberately independent of OIOS's `pending_adjustments`/`shadow_mode` (same conceptual shape reused as a template only — zero imports from `oios/`, zero shared storage; own store at `data/ksl/`). Lifecycle: `SHADOW_ELIGIBLE→SHADOW_ACTIVE→ACTIVE/REJECTED`, plus `ACTIVE→ROLLED_BACK` auto-revert on live degradation — fully automated, no human step. Guardrails mirror OIOS's proven numbers as constants only (bounded weight ≤0.15, cooldown 90 days, min shadow observation days/sample). `annotate_adjusted_scores()` is the additive hook: always computes a parallel shadow score (zero effect on real selection) and only overrides the real ranking when a direction has a live-confirmed `ACTIVE` adjustment | N/A |
| `scripts/final_trading_architecture_shadow_001.py` | RSL-001: `_assign_ranks()` gained one additive, try/except-wrapped call to `annotate_adjusted_scores()` before `select_c2_top_n()`. With zero active adjustments (current state) this is byte-identical to prior behaviour — confirmed via the existing 77-test suite, all passing. This file (not `opportunity_engine/final_c2_selector.py`) is the actual live-running shadow generator that feeds KSL-001; `final_c2_selector.py`'s frozen sort (`valid.sort(key=lambda c: (-c.c2_score, c.v3_rank))`) was confirmed untouched and is only exercised by its own test file, not the production shadow pipeline | No |
| `scripts/knowledge_system/ksl_models.py` | RSL-001: `KSLShadowCandidate` gained 6 new optional/defaulted fields (`feature_id`, `shadow_start_date`, `shadow_activated_at`, `live_activated_at`, `rolled_back_at`, `status_reason`) + a `from_dict()` classmethod — additive only, mirrors the precedent already set by `EvidenceRecord`'s Phase-1 field additions in this same file | No |
| `scripts/knowledge_system/knowledge_feedback_loop_001.py` | RSL-001: added Stage 10 (`validate_pending_hypotheses()`) and Stage 11 (`advance_shadow_tracking()` + `check_rollback()`) to the existing EOD loop, both try/except-wrapped and non-fatal, matching every other stage's safety convention | No |
| `tests/test_ranking_self_learning_001.py` | RSL-001: NEW — 40/40 tests (T001–T040): scorecard statistics, time-split no-leakage invariant, automated hypothesis lifecycle (fake registry, no real files touched), shadow-tracking state machine (activate/promote/reject), auto-rollback, `annotate_adjusted_scores` no-op-vs-active behaviour, forbidden-import safety checks, frozen-selector-untouched assertion | N/A |
| `strategy_lab/strategy_generator_ai.py` | DTA-EQUITY-STRATEGYLAB-OBSERVATION-001: extended the "StrategyLab is observation-only, KDA is sole authority" carve-out (previously options/spread only, DTA-OPTIONS-ISOLATION-001) to EQUITY. `_assign()`'s active-set exclusion check and `assign_strategy()`'s final hard gate no longer `return None`/drop a signal when its strategy is SHM/PerfTracker-disabled — the health verdict is recorded on `TradeSignal.strategy_health_status` (e.g. `SHM_EXCLUDED`, `META_INACTIVE`) and the signal is still forwarded to KDA. Untouched, still-enforced equity gates: bear-market-BUY rejection, RR-below-minimum rejection, volatile-regime-low-confidence rejection — these are structural eligibility rules, not SHM's historical-performance judgement, and were never part of the options carve-out either | No |
| `models/trade_signal.py` | DTA-EQUITY-STRATEGYLAB-OBSERVATION-001: added `strategy_health_status: Optional[str] = None` — additive, observation-only field | No |
| `tests/test_equity_strategylab_observation_only_001.py` | DTA-EQUITY-STRATEGYLAB-OBSERVATION-001: NEW — 9/9 tests confirming equity signals are no longer dropped by SHM/active-set exclusion (T001–T004), and confirming bear-market/RR-minimum gates remain enforced (T005–T006) | N/A |
| `knowledge_authority/kda_constant_refinement_engine.py` | KDA-CRE-001: NEW — fully automated (no human step), self-scheduled refinement of KDA's 3 live evidence-gate constants (`_ESS_DECISION_ELIGIBLE`, `_STABILITY_DECISION_MIN`, `_CONTRADICTION_DECISION_MIN` — confirmed the other 2 declared constants, `_AUTHORITY_KNOWLEDGE_MIN`/`_AUTHORITY_STRATEGY_MIN`, are dead code since ARCH-005). Self-scheduling is evidence-driven (checks re-trigger only after ≥30 new resolved KDA outcomes accumulate) gated by a dynamic cooldown floor calculated from the system's own observed outcome-arrival rate (never below an absolute 30-day safety floor — prevents thrashing on a short burst of correlated evidence). Statistical validation mirrors RHV-001 (time-ordered 70/30 train/OOS split, bootstrap CI, 4-check scorecard) but with a materially higher minimum sample (50 vs RHV-001's 10) since this gates live-money trades. Lifecycle: `WAITING_FOR_EVIDENCE→SHADOW_ACTIVE→ACTIVE/REJECTED`, plus `ACTIVE→ROLLED_BACK` auto-revert on live degradation. Every transition is appended (never overwritten) to `data/kda_cre/constant_change_ledger.jsonl` with full reasoning, for research reuse. Own isolated store at `data/kda_cre/`; zero imports from `oios/`/`scripts/knowledge_system/` | N/A |
| `knowledge_authority/knowledge_decision_authority.py` | KDA-CRE-001: added `_effective_constants()` — additive accessor that reads KDA-CRE-001's validated overrides (mtime-cached) and falls back to the exact original hardcoded defaults on any error/missing file. `_classify_evidence_state()` and `_compute_authority()` now read the ESS/stability/contradiction thresholds through this accessor instead of the bare module constants. Public interface (`evaluate()`, inputs/outputs) completely unchanged; behavior is byte-identical unless a shadow-confirmed override exists | No |
| `orchestrator/master_orchestrator.py` | KDA-CRE-001: added one non-fatal, try/except-wrapped EOD call to `run_daily_refinement_check()` immediately after the existing `run_eod_knowledge_update()` call, matching the established EOD-stage convention | No |
| `tests/test_kda_constant_refinement_engine.py` | KDA-CRE-001: NEW — 26/26 tests (T01–T26): override accessor (defaults/valid/corrupt/out-of-bounds), dynamic cooldown calculation, scorecard statistics (validates real signal, rejects noise, never moves in a worsening direction), full state machine (waiting/cooldown/shadow-start/promote/reject/rollback/no-premature-rollback), top-level safety (`run_daily_refinement_check` never raises), ledger append-only integrity, and integration proof that `knowledge_decision_authority.py` both falls back safely with no override and actually picks up a valid one | N/A |
| `control_tower/dashboard_app.py` | Fixed hardcoded "Mode: 🧪 PAPER" header badge — was a static string, never reflected the real trading mode. `.env` was already mounted into this container (GAP-026) but never actually loaded (no `load_dotenv()` call anywhere in the file); added it plus `_real_trading_mode_label()`, mirroring `OrderManager.__init__`'s own live-mode gate exactly (paper unless BOTH `PAPER_TRADING=false` AND `LIVE_TRADING_AUTHORIZED=true`) | No |
| `ARCHITECTURE.md` / `.github/copilot-instructions.md` | Updated stale "Current mode: Paper trading" headline text to reflect the deliberate 2026-09-14 live-trading decision — was pure documentation drift, never affected runtime behavior | N/A |
| `ARCHITECTURE.md` | Naming-sprawl Plan B, Phase 0 (docs-only, zero import/behavior touched): extended §13 Subsystem Naming Glossary with the acronyms/collisions missing from the first pass — root `knowledge_system/` (options-only, live) vs `scripts/knowledge_system/` (KSL, identical folder name, different package), PRR/PGA/CLE/GVA/HKAP/IKN (all confirmed disconnected from `master_orchestrator.py`), and the "DTA" collision (`decision_tracer/`'s own `dta_*.py` file prefix vs this repo's DTA-XXX ticket convention — same token, two unrelated meanings). Also flagged (not fixed) the dead-after-construction `self.research_lab = ResearchLab()` instantiation as a separate future housekeeping item | No |
| `autonomous_research/__init__.py`, `growth_validator/__init__.py`, `decision_tracer/__init__.py`, `hkap/__init__.py`, `ikn/__init__.py`, `production_readiness/__init__.py`, `predictive_gap/__init__.py` | Naming-sprawl Plan B, Phase 1 (docstring-only, zero import/export/behavior touched): added a one-line "STATUS: DISCONNECTED from live trading... see ARCHITECTURE.md §13" banner to each confirmed-dormant package's module docstring (verified via import smoke test post-edit — all 7 still import cleanly, `__all__`/exports unchanged). `decision_tracer/` additionally notes its `dta_` file prefix is unrelated to the repo's DTA-XXX ticket convention; `hkap/` additionally notes its "reuses IIOS V1.0" premise was never true (`iios/` is only a Wave-1 placeholder) | No |
| `decision_tracer/dta_analyzer.py`→`dtrace_analyzer.py`, `dta_collector.py`→`dtrace_collector.py`, `dta_reporter.py`→`dtrace_reporter.py`, `dta_runner.py`→`dtrace_runner.py`, `decision_tracer/__init__.py` | Naming-sprawl Plan B, Phase 2: renamed `decision_tracer/`'s internal `dta_*.py` modules to `dtrace_*.py` (via `git mv`, history preserved) to remove the collision with this repo's DTA-XXX ticket convention. Confirmed 0 external references anywhere in the repo before renaming (only `__init__.py` + the 4 files themselves referenced the old names; zero test files, zero other packages). Updated all internal imports, docstring module-path headers, and CLI usage examples to match. Verified: `py_compile` + `importlib.import_module` + explicit import of all 7 public symbols (`run_dta`, `collect_trace`, `analyze`, `write_report`, `generate_report`, `TraceBundle`, `DTAAudit`) all succeed post-rename. `data/dta/` output directory and class/function names (e.g. `DTAAudit`) deliberately left untouched — out of this phase's scope (file-name collision only) | No |
| `iios/__init__.py`, `ARCHITECTURE.md` | Naming-sprawl Plan B, Phase 3 (docs/docstring-only, zero behavior touched): added a one-line clarifying note to `iios/__init__.py` distinguishing it from `oios/` (verified `__version__`/`__status__`/`__wave__`/`__all__` all unchanged post-edit). Added a standing "going forward" rule to `ARCHITECTURE.md` §13: never use "IIOS" as an informal nickname for this project in any NEW doc/docstring — pre-2026-09-14 docs that already do this are left as historical artifacts, not retroactively edited | No |
| `iios/` → `enterprise_ai_platform/` (whole package, ~5,626 tracked files, via `git mv`) + all internal imports + `tests/ai/`, `tests/unit/*`, `tests/certification/`, `tests/performance/` (mirrored test suites) + `pyproject.toml` + `.pre-commit-config.yaml` + `bootstrap.py`/`dev.py`/`run.py`/`healthcheck.py` + `hkap/__init__.py` + `scripts/check_oios_duplicate_symbols.py` + `scripts/dep_check.py` + `ARCHITECTURE.md` §13 | Naming-sprawl Plan B, Phase 4 (user-authorized actual rename, the only in-scope target — `oios/`/`autonomous_research/`/`knowledge_system/`/`knowledge_authority/` remain untouched, still live and out of scope): mechanically renamed the dormant `iios/` package to `enterprise_ai_platform/` (case-sensitive whole-word `\biios\b` → `enterprise_ai_platform` via a one-time script, uppercase "IIOS" prose/env-var-prefix/cert-code usages deliberately left untouched, matching Phase 3's precedent). True scope was ~15x larger than originally estimated (5,626 tracked files vs the ~350 first assumed) — discovered mid-execution via `pyproject.toml`'s own package config (`name = "iios"`, CLI entry points, mypy/bandit hook paths) and a full `tests/unit/*` mirror tree; proceeded since the risk profile (zero live import sites) was unchanged by the larger scope. Verified: `python -m compileall` clean on every touched file; `pytest --collect-only` across the full suite → 34,554 tests collected, 0 collection errors; ran a 361-test execution sample (`tests/unit/bootstrap` + `tests/ai/foundation`) → 360 passed, 1 failed for a pre-existing, unrelated reason (a bootstrap test asserts `paper_trading==True` by default but this repo's real `config.py` is intentionally `PAPER_TRADING=False` since the 2026-09-14 live-trading decision — not caused by the rename, not fixed here, out of this phase's scope). Historical root `.md` reports (A1–A10, `AI_PLATFORM_*.md`, root `README.md`, etc, ~70 files) deliberately left untouched per the established "don't retroactively edit historical docs" precedent | No |
| `risk_control/options_risk_engine.py` | Self-Learning Ecosystem Phase 1: wired `OptionsKnowledgeStore.get_influence()` (already existed, already used for confidence scoring in `options_opportunity_ai.py`) into `approve_and_size()`'s lot-sizing decision — a NEW consumer, not previously wired here (confirmed via prior grep: options_risk_engine.py had zero references to the knowledge store before this change). Size-UP (+1 lot) gated on the stricter `KS_AUTHENTICATED` tier only (≥40 real outcomes + walk-forward Sharpe>0, already live/automatic/zero-human-step) and always re-checked against the existing capital/capacity hard caps before applying. Size-DOWN (−1 lot, floor 1) on `KS_DEGRADED` applies unconditionally since reducing risk on decaying edge is always safe. No new parallel validation/shadow system introduced — deliberately reuses the pre-existing, already-evidence-gated state machine as the sole trust signal, applied at a stricter tier than the existing confidence consumer since sizing is a more consequential lever. Wrapped in try/except (fail-open); all 4 pre-existing risk gates (capital/capacity/VIX/loss-streak) run unchanged before and after this block | No |
| `tests/test_options_ks_bridge_phase1.py` | Self-Learning Ecosystem Phase 1: NEW — 10/10 tests (T01–T10): zero effect with no/insufficient knowledge, size-up bounded to exactly +1 lot only at KS_AUTHENTICATED with positive influence, size-up never exceeds the hard lot cap or remaining capital capacity, size-down on KS_DEGRADED never below 1 lot, bridge exception is fail-open. Full options regression suite (242 tests across knowledge lifecycle, phase2/3 hardening, rollback safety, live execution, MOP-RC-001) re-run clean after the change | N/A |
| `predictive_gap/pga_learning.py` | Self-Learning Ecosystem Phase 3: fixed a real, pre-existing bug in `_try_create_hypothesis()` (Category C) — was calling `HypothesisRegistry.create_hypothesis()` with parameters that don't exist on that method (`rationale=`, `tags=`) and an invalid enum member (`HypothesisClassification.PREDICTIVE_SIGNAL` doesn't exist), so every "auto-executed" Category C hypothesis creation had been silently failing (caught by a bare `except`, logged at DEBUG, outcome recorded as `HYPOTHESIS_FAILED`) since this file was written. Fixed to use the real required parameters (`description`, `origin`, `knowledge_gap`, `expected_knowledge_gain`, `validation_method`) and a valid classification (`COVERAGE_GAP`). Added Category A wiring (`_try_create_hypothesis_cat_a`): registers RiskFilter/PMCI misses as a `PROPOSED` hypothesis for future validation — deliberately does **not** touch `risk_control/risk_manager_ai.py`'s `MIN_RR_RATIO` or any scanner weight directly, because PGA's own miss-classification (`pga_analyzer.py::_classify_miss_type()`) explicitly excludes non-significant-move rejections (`MISS_NO_DATA`), meaning Category A evidence is survivorship-biased (only ever sees rejections that became big movers, never the ones correctly avoided) — not sound enough to auto-tune a live risk/scanner gate. Also noted but NOT fixed (out of scope for this phase): Category B's `_try_reinforce_idr()` calls `IDRRepository.add_observation()`, a method that doesn't exist on that class either — same silent-failure bug, a separate pre-existing issue | No |
| `autonomous_research/ptue.py`, `autonomous_research/ptue_models.py` | Self-Learning Ecosystem Phase 3 (incidental bug fix, found while verifying the above): replaced 2 deprecated `datetime.utcnow()` calls with `datetime.now(timezone.utc)` — Python 3.12+ deprecation, was tripping this repo's own strict `filterwarnings=["error", ...]` pytest policy (`pyproject.toml`) whenever `autonomous_research` was the first thing imported in a test session, causing intermittent, import-order-dependent test failures. Verified via `test_ptue.py` (its own standalone smoke-test script, run directly): 156/156 pass | No |
| `tests/test_pga_category_a_hypothesis_bridge.py` | Self-Learning Ecosystem Phase 3: NEW — 8/8 tests (T01–T08): Category A creates a real, correctly-classified (`PERFORMANCE_GAP`, priority `LOW`) hypothesis in `PROPOSED` status with the survivorship-bias caveat in its own description text; Category C's fix is proven by a regression test; `execute_actions()` routes Category A to `HYPOTHESIS_CREATED_FOR_VALIDATION`; bridge exception is fail-open; `risk_control.risk_manager_ai.MIN_RR_RATIO` is asserted unchanged after running the bridge. Caught and fixed a real test-isolation bug during development: my first test run used the default (unpatched) `HypothesisRegistry` path and wrote 7 real test hypotheses into the production `data/ars_hypothesis_registry.json` — caught immediately, restored the file to its original 16 entries from the pre-existing `.bak`-plus-manual-diff, and fixed the test fixture to patch `autonomous_research.hypothesis_registry._DEFAULT_REGISTRY_PATH` (read fresh per-call, not bound at import time) instead of relying on `chdir` | N/A |
| `autonomous_research/hypothesis_models.py` | Self-Learning Ecosystem Phase 2 structural prerequisite: added 3 new **optional** fields to `ScientificHypothesis` (`subject_type`, `subject_value`, `direction`) so a hypothesis can reference a concrete symbol/strategy/regime — closes the "no structured subject field" blocker found during the original Phase 2 audit. `to_dict()`/`from_dict()` both `.get()`-default, fully backward compatible with existing production `data/ars_hypothesis_registry.json` (verified) | No |
| `autonomous_research/hypothesis_registry.py` | Self-Learning Ecosystem Phase 2 structural prerequisite: `create_hypothesis()` gained optional `subject_type`/`subject_value`/`direction` kwargs; NEW `get_confirmed_adjustment(subject_type, subject_value, max_delta=0.05)` — bounded, sign-matched, multi-match-averaged accessor over `CONFIRMED` hypotheses only. Dormant today (returns `0.0`) since no hypothesis has yet reached `CONFIRMED` with these fields populated — deliberately NOT wired into `knowledge_authority/knowledge_decision_authority.py`; that wiring is a separate, explicitly-approved contract per this repo's stricter KDA change workflow, not done in this pass | No |
| `predictive_gap/pga_learning.py` | Self-Learning Ecosystem Phase 2 structural prerequisite: `_try_create_hypothesis_cat_a()` now also passes `subject_type="SYMBOL"`, `subject_value=<symbol>`, `direction="POSITIVE"` into `create_hypothesis()`, giving Category A evidence the structured shape a future KDA consumer needs | No |
| `tests/test_kda_bridge_phase2_structural.py` | Self-Learning Ecosystem Phase 2 structural prerequisite: NEW — 9/9 tests (T01–T09): backward-compat defaults-to-None, `from_dict` handles pre-existing-shape dicts, `create_hypothesis` persists subject fields, `get_confirmed_adjustment` returns `0.0` with no match and with matching-but-not-`CONFIRMED` status (incl. `VALIDATED`), correctly bounded +/− delta for `CONFIRMED`+`POSITIVE`/`NEGATIVE`, averages+clamps multiple matches, PGA Category A populates `SYMBOL`/subject_value/`POSITIVE` correctly. Combined regression (structural + Phase 1 + Phase 3): 27/27 pass; standalone `test_hypothesis_registry.py` (own 40-test suite) re-run clean; production registry files confirmed untouched | N/A |
| `knowledge_authority/knowledge_decision_authority.py` | Self-Learning Ecosystem Phase 2 (KDA wiring, explicit contract approved before implementation): `_compute_authority()` now reads `HypothesisRegistry(knowledge_provider=KnowledgeProvider()).get_confirmed_adjustment("SYMBOL", symbol, max_delta=0.05)` and nudges the existing `relevance` component only (re-clamped to its own pre-existing `[0.1, 1.0]` bound) — never touches `evidence_state`, `_determine_decision()`'s categorical BUY/SELL/HOLD/WAIT path, `_classify_authority()`, or any other component; `KnowledgeAuthorityComponents` (still exactly 6 components + composite) untouched. Fail-open (`except Exception: ars_adj = 0.0`); dormant on deploy (returns `0.0` for every symbol until a hypothesis actually reaches `CONFIRMED` with subject fields set — none do in production yet) | No |
| `tests/test_kda_phase2_bridge.py` | Self-Learning Ecosystem Phase 2 (KDA wiring): NEW — 5/5 tests (T01–T05): zero effect today with an empty isolated registry, bounded `+delta = max_delta * confidence` for a fabricated `CONFIRMED` `POSITIVE` match, `relevance` never leaves `[0.1, 1.0]` at either extreme, fail-open when `HypothesisRegistry` raises, `composite_authority` stays in `[0, 1]`. Full existing KDA regression re-run clean: 416/416 pass (`test_kda_001/002/003.py`, `test_dta_030_provenance.py`, `test_dta_system_014/015.py` — including T037's multiplicative-dampening invariant). Live-path smoke test (`import orchestrator.master_orchestrator`) clean | N/A |
| `analysis/rejection_attribution_monitor.py` | Self-Learning Ecosystem Phase 4 (persistent rejection-attribution monitor): NEW — closes a real, pre-existing gap: `RejectionTracker.ingest_rejection()` has been live-wired at 4 real rejection points (CapitalRiskEngine x2, StrategyLab, RiskControl PositionAllocator/StressTest) since DTA-ATTRIBUTION-TRAIL-001, but `RejectionTracker.update_price_follow()` was never called from anywhere in production, so every real rejection sat `PENDING` forever. `RejectionAttributionMonitor.run_daily_cycle()` resolves `PENDING` rows >=7 calendar days old via real yfinance T+1/T+3/T+5 close prices (same tested fetch pattern as `opportunity_engine/klp_outcome_engine.py::_fetch_ohlcv_yfinance`), then recomputes per-reason accuracy via the existing `accuracy_by_reason()`. `get_reason_reliability()` is a read-only accessor gated on `MIN_SAMPLES_FOR_RELIABILITY=10` (mirrors `StrategyPerformanceTracker.MIN_SAMPLE`), returns `None` below that sample size — advisory-only, NOT wired into any live risk/decision gate this phase (this repo's own earlier, explicitly-flagged finding that `MAX_POSITIONS_CAP`'s 48% "accuracy" claim was synthetic-data-only is exactly why a real per-reason figure needs its own dedicated evidence first). Writes an append-only `data/rejection_attribution/daily_summary.jsonl`. Never raises; zero imports from execution_engine/order_manager/broker APIs | No |
| `orchestrator/master_orchestrator.py` | Self-Learning Ecosystem Phase 4: added one non-fatal, try/except-wrapped EOD call to `run_daily_rejection_attribution()`, matching the established EOD-stage convention (same block style as KDA-CRE-001) | No |
| `tests/test_rejection_attribution_monitor.py` | Self-Learning Ecosystem Phase 4: NEW — 8/8 tests (T01–T08): immature rows skipped without ever calling the price fetcher, mature rows resolved with the exact T+1/T+3/T+5 closes passed through, no-data rows stay `PENDING`, reliability accessor gated correctly below/at `MIN_SAMPLES_FOR_RELIABILITY`, `run_daily_cycle` fail-open when the tracker itself raises, daily summary JSONL append verified, source-text safety scan confirms zero `execution_engine`/`order_manager`/`dhan_feed`/`broker` imports | N/A |
| `.gitignore` | Added `data/rejection_attribution/` (new runtime output directory for Phase 4) | No |
| `analysis/rejection_audit.py` | Root-cause fix: `seed_synthetic_data()` was passing `is_backfill=False` for its SYNTHETIC seed rows — meant that column could never distinguish synthetic from real data if this manual CLI (`python analysis/rejection_audit.py --reseed`) were ever pointed at the production db. Fixed to `is_backfill=True`, matching the established convention already used by `trade_quality_tracker.py`'s `backfill_from_paper_trades()`. Manual CLI only, never called from any scheduled/live path — zero behavior change to any live trading path | No |
| `analysis/rejection_attribution_monitor.py` | Following the above root-cause fix: `_compute_reliability_summary()` and `get_reason_reliability()` now filter out `is_backfill=1` rows, so synthetic/backfilled evidence can never contaminate a real per-reason reliability figure (previously undocumented as a caveat since the source bug made the filter unreliable; now safe to apply) | No |
| `tests/test_rejection_attribution_monitor.py` | Added T09 (`is_backfill=1` rows excluded from reliability computation even at/above sample threshold) and T10 (regression test proving `rejection_audit.py`'s seeded rows are now marked `is_backfill=1`) — 10/10 pass | N/A |
| `production_readiness/prr_monitor.py` | Self-Learning Ecosystem Phase 5: NEW — closes the same "computed but discarded" gap found in Phase 3/4: `prr_runner.run_prr()` already computes a daily certification summary (`certification_status`/`ils_score`/`gva_score`/`critical_failures`/`warnings`) at EOD, but the dict was discarded after one `log.info()` line. `record_daily_result()` persists it to an append-only `data/prr/daily_summary_history.jsonl`; `get_latest_certification()`/`get_certification_history()` are read-only accessors for a future dashboard/Telegram command; `check_and_alert()` sends a `notifier.market_alert()` Telegram push only on `NOT_READY` or a verdict CHANGE since the last recorded entry (mirrors the existing KDA Authority Update "alert only on state change" convention) — advisory only, never touches any trade/risk/decision gate. Found but explicitly NOT fixed (separate, bigger, out-of-scope task): `ph5_daily_pipeline.py::run_daily_pipeline()` is dead code, never called anywhere despite its own docstring claiming otherwise, so `ph9_certification.py`'s `Daily_ILC_Operational` check always evaluates a hard-coded `pipeline=None` PASS rather than a real pipeline-health signal — fixing that would change PRR's own certification *logic*, not just surface its existing output | No |
| `production_readiness/__init__.py` | Phase 5: added `prr_monitor` re-exports (`record_daily_result`, `get_certification_history`, `get_latest_certification`, `check_and_alert`), matching this package's existing re-export-everything convention | No |
| `orchestrator/master_orchestrator.py` | Phase 5: added one non-fatal, try/except-wrapped call to `record_daily_result()` + `check_and_alert()` immediately after the existing `[PRR-001]` log line, matching the established EOD-stage convention | No |
| `tests/test_prr_monitor.py` | Phase 5: NEW — 9/9 tests (T01–T09): history append + read-back (oldest-first, last-n), latest-accessor None-when-empty and correct-when-populated, alert fires on `NOT_READY` and on verdict change, alert suppressed when unchanged and ready, fail-open when the notifier itself raises, source-text safety scan confirms zero `execution_engine`/`order_manager`/`dhan_feed`/`broker`/`risk_control` imports | N/A |
| `production_readiness/ph5_daily_pipeline.py` | Root-cause fix: added `build_pipeline_result_from_live_stages()` — builds a real `DailyPipelineResult` from the orchestrator's OWN already-executed PGA/ILC result dicts, never re-running anything (confirmed via grep that GVA/SD_review/ILC-verification are never called live, so those sub-fields correctly stay `None` rather than being fabricated). Corrected the module's stale/false docstring claim that `run_daily_pipeline()` (which actually RE-RUNS PGA→ILC→GVA→SD→verification→reports) is "called from `_do_eod_learning()`, replaces the individual try/except wrappers" — it never was, and wiring it in as originally described would have duplicated PGA/ILC execution; `run_daily_pipeline()` is kept as a standalone/manual utility, untouched | No |
| `production_readiness/prr_runner.py` | Root-cause fix: `_collect_prr_data()`/`run_prr()` gained an optional `pipeline` parameter (default `None`, fully backward-compatible — CLI and all existing callers unaffected). Previously `data.setdefault("pipeline", None)` was the only place `data["pipeline"]` was ever touched, so it was permanently `None`; now the orchestrator passes a real, non-duplicated `DailyPipelineResult` through | No |
| `production_readiness/ph9_certification.py` | Root-cause fix: `Daily_ILC_Operational` check's narrative text no longer hard-codes `"/6"` (misleading once only 2 of 6 possible stages are ever populated) — now reports the actual attempted stage total. The check itself now reflects a REAL PGA/ILC outcome instead of always defaulting to the "not yet run" `INFO`-only placeholder | No |
| `production_readiness/__init__.py` | Root-cause fix: added `build_pipeline_result_from_live_stages` to the re-export list, matching this package's existing convention | No |
| `orchestrator/master_orchestrator.py` | Root-cause fix: `[PRR-001]` block now builds a `DailyPipelineResult` from the same-cycle `_pga`/`_ilc` locals (via `"_pga" in locals()` / `"_ilc" in locals()`, matching this file's own existing convention for "was this local set earlier this cycle" checks) and passes it into `run_prr(pipeline=...)` — never re-runs PGA/ILC, wrapped in its own inner try/except so a builder failure can never break the existing PRR pipeline call | No |
| `tests/test_prr_pipeline_root_cause.py` | Root-cause fix: NEW — 7/7 tests (T01–T07): builder correctly reflects real PGA/ILC success/failure and leaves untouched stages `None`; `ph9_certification.build_certificate()` now issues a real `CRITICAL` fail when ILC actually failed and a real `PASS` with an accurate (non-hard-coded) stage-count narrative when it succeeded; `run_prr(pipeline=None)` default behavior fully unchanged; `_collect_prr_data()` correctly threads the `pipeline` argument through to the certificate | N/A |
| `hkap/snapshot_builder.py` | Self-Learning Ecosystem Phase 6: root-cause fix — `HistoricalSnapshotBuilder._load_or_download()` crashed on every real yfinance call (`only 0-dimensional arrays can be converted to Python scalars`) because newer yfinance returns MultiIndex columns even for a single ticker, and numpy 2.x's `float()` rejects the resulting non-0-d arrays. Fixed with the same MultiIndex-droplevel pattern already proven in `opportunity_engine/klp_outcome_engine.py`. This was blocking 100% of real HKAP downloads — reproduced and verified fixed before/after with a live yfinance call | No |
| `hkap/hkap_engine.py` | Self-Learning Ecosystem Phase 6: added `HKAPEngine.get_completed_packages()` — small, read-only accessor returning already-persisted/loaded `COMPLETE`-status year packages (never triggers a download or run), used by the new HKAP→KDE bridge | No |
| `hkap/hkap_kde_bridge.py` | Self-Learning Ecosystem Phase 6: NEW — bridges HKAP's real output into `kde/` (Knowledge Discovery Engine), the structurally correct consumer discovered during audit (confirmed via `test_kde.py` to accept exactly HKAP's `hkap_packages`/`dna_records`/`edge_records` shape) — NOT `HBE`/`KLP` as the roadmap's one-line description originally assumed (see audit finding below). `run_hkap_kde_discovery()` loads already-persisted HKAP packages (no download), runs `CrossYearAnalyzer` + `KDEEngine` (both pre-existing, unchanged), and persists an append-only `data/kde/bridge/run_history.jsonl`; `get_latest_discovery_run()`/`get_discovery_run_history()` are read-only accessors. Deliberately NOT wired into `master_orchestrator.py`'s daily EOD loop (HKAP/KDE are a periodic multi-year historical computation, not a daily live-trading event) and NOT wired into HBE/KDA (would require fabricating trade-outcome fields HKAP never computes — same risk already rejected for PGA Category A in Phase 3). Advisory/research-only, callable on whatever cadence a human or future scheduler chooses | No |
| `hkap/__init__.py` | Phase 6: added `hkap_kde_bridge` re-exports (`run_hkap_kde_discovery`, `get_latest_discovery_run`, `get_discovery_run_history`), matching this package's existing re-export convention | No |
| `scripts/hkap/seed_hkap_years.py` | Phase 6: NEW — manual/periodic utility to populate real `data/hkap/{year}/year_knowledge_package.json` via genuine yfinance downloads (small, bounded scope by default: 2023–2024, 40 symbols). Not part of the live path; `hkap_kde_bridge.py` only reads what this script has already persisted, never downloads anything itself. First real run (post-fix) completed 2023+2024 successfully, producing genuinely non-fabricated `SectorRotationScheme` discoveries | N/A |
| `tests/test_hkap_kde_bridge.py` | Phase 6: NEW — 7/7 tests (T01–T07): `INSUFFICIENT_YEARS` short-circuit never calls `KDEEngine`; correct `CrossYearAnalyzer`+`KDEEngine` wiring with a correctly-shaped summary; history read-back oldest-first; fail-open when `HKAPEngine()` itself raises; `get_completed_packages()` only returns `COMPLETE`-status packages; source-text safety scan confirms zero `execution_engine`/`order_manager`/`dhan_feed`/`broker`/`risk_control`/`knowledge_authority` imports; `snapshot_builder` root-cause fix verified against a mocked MultiIndex-columned DataFrame. Full existing `test_hkap.py` standalone suite re-run clean: 100/100 pass | N/A |
| `.gitignore` | Added `data/hkap/` and `data/kde/` (new runtime output directories for Phase 6) | No |
| `autonomous_research/ars_scheduler.py` | Post-roadmap Priority 1: NEW — activates the 9-agent autonomous_research (ARS) cluster (ResearchCoordinator, ScientificDirector, GapDetector, RoadmapManager, EvidenceValidator, StudyPlanner, CrossStudySynthesizer, MethodologyAuditor, ScientificFindingsReview/DataQualityAssessor) that was fully built (190+ tests) but never invoked in production. Audit found the ENTIRE autonomous decision chain already existed and was already tested — `ScientificDirector.weekly_review()` already auto-generates hypotheses for open gaps and auto-approves low-risk "Class A" study plans (`SDConfig.auto_approve_class_a=True` by default), which on approval directly calls `ResearchCoordinator.run_research(plan)` (already-existing code in `scientific_director.py`'s own `approve_study()`). `StudyPlanner.create_from_entry()` already existed to turn a prioritized `RoadmapEntry` into a `StudyPlan`. This module invents ZERO new statistical/governance logic — it is pure wiring (constructs all 9 components together, something nothing in production ever did) plus a self-scheduling weekly cadence guard (own run-history log, skips silently if <7 days since last real run — research is not a daily event, same precedent as HKAP/KDE). Verified end-to-end against REAL production data: found 5 real gaps, created 5 real StudyPlans, ran `weekly_review()` for real — auto-created 3 hypotheses (scientific_director), auto-approved+executed 5 Class A study plans through the full 8-stage `ResearchCoordinator` pipeline, which itself auto-created 2 more hypotheses via `ScientificFindingsReview` (origin=`SFR_AUTO`) — genuinely activating all 9 previously-dormant agents in one real run. All new hypotheses land in `PROPOSED` status (not auto-trusted) and are gap-based (`subject_type` unset), so they do NOT feed into KDA's `get_confirmed_adjustment()` symbol-adjustment bridge — this cluster surfaces broader research findings for the existing RHV-001 validation lifecycle to review, it does not silently retune any live trading signal. `ResearchCoordinator`'s `repository_update` stage confirmed read-only via source inspection (only `.statistics()`/`.list_active()`/`.list_studies()`/`.list_edges()` calls) | No |
| `orchestrator/master_orchestrator.py` | Post-roadmap Priority 1: added one non-fatal, try/except-wrapped EOD call to `run_autonomous_research_cycle()`, matching the established EOD-stage convention. Self-skips most days via its own 7-day cadence guard | No |
| `sandy/sandy_supervisor.py` | Post-roadmap Priority 1: added `_poll_ars_scheduler()` as an 11th Sandy poller, matching the existing pattern exactly | No |
| `tests/test_ars_scheduler.py` | NEW — 8/8 tests (T01–T08): self-scheduling skip/force/first-run behavior, full 9-component wiring verified (constructor kwargs, cross-references), no duplicate plans for gaps that already have one, run-history read-back oldest-first, fail-open on component exception, source-text safety scan. Combined Sandy regression (with the new 11th poller): 37/37 pass | N/A |
| `.gitignore` | Added `data/ars/scheduler/`, `data/ars/sfr/`, `data/ars/sd/`, `data/ars_roadmap_state.json` (new real runtime output directories produced by the now-active ARS cluster) | No |
| `ikn/ikn_research_bridge.py` | Post-roadmap Priority 2: NEW — activates IKNNetwork (`ikn/`), previously only used by standalone scripts (kmp.py/kva.py/svp.py) with zero live callers. `IKNNetwork`'s own docstring already names its intended writers ("ResearchCoordinator, KDE, HKAP, CrossStudySynthesizer") — it is a pure knowledge-graph store, `register_node()`/`add_relationship()` are simple, safe, idempotent-by-design operations, and nothing anywhere reads IKN back into a live decision (unlike IDR). `sync_hypotheses_to_ikn()` mirrors every hypothesis already produced by the ARS scheduler (Priority 1) into IKN as a HYPOTHESIS node (+ a FINDING node for its `knowledge_gap`, if any, + a GENERATED_BY relationship) — giving IKN real, meaningful graph data for the first time instead of adding an observability poller over a permanently-empty store. Idempotent: `register_node()` upserts by node_id; relationships are only added if an identical one isn't already found via `get_relationships()` first. Verified against real production data: 21 real hypotheses mirrored, 82 total nodes / 39 relationships now queryable via `IKNNetwork.statistics()` | No |
| `ikn/__init__.py` | Added `sync_hypotheses_to_ikn` re-export | No |
| `autonomous_research/ars_scheduler.py` | Post-roadmap Priority 2: `_run_impl()` now also calls `ikn.ikn_research_bridge.sync_hypotheses_to_ikn()` after `weekly_review()` (try/except-wrapped, non-fatal); result embedded in the cycle summary as `ikn_sync` | No |
| `sandy/sandy_supervisor.py` | Post-roadmap Priority 2: added `_poll_ikn_bridge()` as a 12th Sandy poller (reads `IKNNetwork.statistics()` directly — real node/relationship/avg_confidence counts) | No |
| `tests/test_ikn_research_bridge.py` | NEW — 8/8 tests (T01–T08): hypothesis mirrored with correct metadata, gap creates a FINDING node + GENERATED_BY relationship, no-gap creates no relationship, idempotent (no duplicate relationship on re-sync), `limit=` respected, fail-open on registry/IKN construction exceptions, source-text safety scan. Full standalone `test_ikn.py` suite re-run clean: 271/271 pass (unmodified, confirms IKN package itself untouched) | N/A |
| `tests/test_ars_scheduler.py`, `tests/test_sandy_supervisor.py` | Updated for the new IKN sync call / 12th poller: added `_stub_ikn_sync` fixture + T09 (IKN sync called + fails open); bumped hardcoded poller-count assertions from 11 to 12 | N/A |
| `debate_system/debate_vote_tracker.py` | Post-roadmap Priority 3 (part 1 of 2): NEW — ACQUISITION+VALIDATION for MultiAgentDebate weight self-tuning. Audit found NO per-debater outcome evidence existed anywhere (DecisionEngine's own `_log_scorecard()` only logs, never persists) — auto-tuning `AGENT_WEIGHTS` on zero evidence would violate this ecosystem's own founding principle. `record_debate_votes()` persists every debate's per-agent votes (called additively from `master_orchestrator.py`, never touches `DecisionEngine.decide()` itself); `resolve_matured_votes()` resolves 7-day-matured records via real yfinance T+1..T+5 closes (mirrors the exact tested pattern in `analysis/rejection_attribution_monitor.py`, works even for REJECTED signals since a debate is evaluated independent of execution), marking each vote `correct` if the debater's own score (>=6.5 individually-approve) agreed with the realized directional outcome. `get_debater_accuracy()`/`get_resolved_records_for_agent()` are read-only accessors consumed by the refinement engine | No |
| `debate_system/debate_weight_refinement_engine.py` | Post-roadmap Priority 3 (part 2 of 2): NEW — SYNTHESIS+GOVERNANCE, mirroring KDA-CRE-001/RSL-001's exact shadow-then-live lifecycle (`WAITING_FOR_EVIDENCE→SHADOW_ACTIVE→ACTIVE/REJECTED`, plus `ACTIVE→ROLLED_BACK` on post-activation degradation — fully automated, no human step). Higher evidence bar than other phases since this gates a live-money vote weight directly: `MIN_SAMPLE_FOR_VALIDATION=50` (mirrors KDA-CRE-001's own higher-bar reasoning), time-ordered 70/30 train/OOS split, bootstrap CI (1000 iters) vs the 0.50 null, sign-consistency + minimum effect magnitude checks. Bounded: `MAX_WEIGHT_DELTA=±0.05`, cannot dominate a debater's original weight. Shadow reconfirmation uses a lighter directional check on the NEW held-out evidence only (deliberately not the full initial-validation sample-size bar, which shadow-window evidence alone could never reach). `get_effective_weights()` mirrors `knowledge_decision_authority.py`'s `_effective_constants()` accessor exactly (mtime-cached, falls back to hardcoded `DEFAULT_WEIGHTS` on any error) | No |
| `decision_ai/decision_engine.py` | Post-roadmap Priority 3: added `_effective_weights()` accessor (mirrors KDA's `_effective_constants()` precedent exactly) — both `decide()`'s weighted-score loop and `_log_scorecard()`'s logging now read through this accessor instead of the bare `AGENT_WEIGHTS` dict. Verified byte-identical behavior today (confirmed `_effective_weights() == AGENT_WEIGHTS` with no override file present) — zero live effect until real evidence validates a debater's weight nudge. `decide()`'s core logic, hard-reject handling, VIX-adaptive threshold, and all other behavior completely unchanged | No |
| `orchestrator/master_orchestrator.py` | Post-roadmap Priority 3: `_run_debate_and_decide()` gained one non-fatal, try/except-wrapped call to `record_debate_votes()` immediately after `decision_engine.decide()` returns (before Market Truth Governance mutations). `_do_eod_learning()` gained two non-fatal calls: `resolve_matured_votes()` then `run_daily_refinement_check()`, matching the established EOD-stage convention | No |
| `sandy/sandy_supervisor.py` | Post-roadmap Priority 3: added `_poll_debate_weight_refinement()` as a 13th Sandy poller (reads per-debater lifecycle status + resolved-vote counts) | No |
| `tests/test_debate_vote_tracker.py` | NEW — 10/10 tests (T01–T10): record shape correctness, fail-open on missing symbol, immature records never trigger a price fetch, BUY/SELL directional correctness rules, no-price-data stays pending for retry, per-debater accuracy aggregation, time-ordered per-agent accessor, fail-open on corrupt file, source-text safety scan | N/A |
| `tests/test_debate_weight_refinement_engine.py` | NEW — 12/12 tests (T01–T12): default weights with no override, insufficient-sample stays WAITING, strong positive/negative signals start SHADOW with correctly-signed bounded deltas, shadow blocked until enough new evidence, shadow promotes to ACTIVE on reconfirming evidence (live weight then reflects the bounded delta), shadow REJECTS on contradicting new evidence, ACTIVE auto-ROLLS_BACK on post-activation degradation, delta always bounded even at 100%/0% accuracy, cooldown blocks immediate re-evaluation, fail-open on tracker import error, source-text safety scan confirms zero forbidden imports AND zero direct import of `decision_ai` (this module never reads the live `AGENT_WEIGHTS` dict directly). Combined regression: 235/235 pre-existing DecisionEngine/debate tests + 68/68 new/updated self-learning-ecosystem tests all pass. Verified end-to-end against real production data: `_effective_weights() == AGENT_WEIGHTS` confirmed byte-identical (no override yet), real `run_daily_refinement_check()` run shows all 6 debaters correctly `WAITING_FOR_EVIDENCE` with `n=0` (honest — no debate has been recorded in production yet, this is the first activation) | N/A |
| `.gitignore` | Added `data/debate/` (new runtime output directory for Priority 3 activation) | No |
| `predictive_gap/pga_learning.py` | Self-Learning Ecosystem Phase 7a: added `get_last_run_summary(pga_dir=None)` — read-only accessor summarizing the most recently written `pga_learning_actions.json` report (by-category/by-outcome counts). Never triggers a new PGA run | No |
| `knowledge_authority/kda_constant_refinement_engine.py` | Phase 7a: added `get_ledger_history(n=20)` (last n `constant_change_ledger.jsonl` events, oldest-first) and `get_refinement_status()` (per-constant lifecycle status + effective value). Both read-only, never trigger a refinement check | No |
| `scripts/knowledge_system/ranking_adjustment_engine_001.py` | Phase 7a: added `get_active_adjustments_status()` — read-only summary of shadow candidates + currently ACTIVE per-direction adjustments. Never mutates any state | No |
| `sandy/__init__.py`, `sandy/sandy_models.py`, `sandy/sandy_supervisor.py` | Self-Learning Ecosystem Phase 7a: NEW — "Sandy" Master Meta-Learning Supervisor core (data-gathering only, no Telegram/chat wiring yet — that's Phase 7b/7c). Polls 10 agents (Phases 1–6 + strategy_performance_tracker + regime_strategy_map baselines) via their own existing read-only accessors (plus the 3 new ones above), classifies each one's activity trend (ACTIVE/IDLE/UNKNOWN) by comparing evidence_count against an append-only `data/sandy/health_history.jsonl` snapshot, and answers fixed-keyword queries ("hi sandy", "sandy report", "sandy status \<name\>", "sandy help") with template-based (no LLM) replies. Read-only w.r.t. every other agent's actual logic/state — never mutates, never trades, never bypasses an agent's own validation/evidence gate. Verified end-to-end against real live data across all 10 agents | No |
| `tests/test_sandy_supervisor.py` | Phase 7a: NEW — 14/14 tests (T01–T10): all pollers report correctly and one failing poller never blocks the others (caught and fixed a real bug: `poller.__name__` access on a mocked callable raised `AttributeError`, silently collapsing the whole poll cycle to `{}` — fixed with `getattr(poller, "__name__", repr(poller))`); trend classification UNKNOWN/ACTIVE/IDLE all verified; all 4 keyword forms route correctly; unrecognised agent name and `daily_digest()` failure are both graceful; source-text safety scan confirms zero `execution_engine`/`order_manager`/`dhan_feed`/`broker`/`risk_control` imports; the 3 new accessors verified against real written data. Combined regression: 88/88 pass (PGA + KDA-CRE-001 + RSL-001 + Sandy); full `test_kda_00{1,2,3}.py` suite re-run clean: 266/266 pass. Live-path smoke test clean | N/A |
| `.gitignore` | Added `data/sandy/` (new runtime output directory for Phase 7a) | No |
| `notifications/telegram_bot.py` | Self-Learning Ecosystem Phase 7b: `_handle_update()` gained a new free-text keyword-routing branch (checked after the `/start`/unauthorized-chat_id checks, before slash-command dispatch) recognizing "hi sandy"/"sandy"/"sandy report"/"sandy status \<name\>"/"sandy help" and routing to `sandy.get_sandy_supervisor().answer()`. Added `_sandy_loop()` — hourly background thread (mirrors the existing `_reminder_loop()` convention) that refreshes Sandy's internal health snapshot even when nobody asks it anything, so IDLE/trend detection reflects real elapsed time; started alongside the existing poll/reminder threads in `start()`. Never sends a message on its own — read-only refresh only | No |
| `orchestrator/master_orchestrator.py` | Phase 7b: added a Sandy EOD digest block at the very end of `_do_eod_learning()` (after the existing `DTA-MARKET-BENCHMARK-001` block, so it reflects the freshest state from the whole day's cycle) — folds `SandySupervisor.daily_digest()` into the existing `notifier.market_alert()` Telegram push, not a new alert type or schedule | No |
| `tests/test_sandy_telegram_integration.py` | Phase 7b: NEW — 9/9 tests (T01–T09): all 3 Sandy keyword forms route correctly and call `answer()` with the expected lowercased query; unauthorized chat_id is rejected **before** Sandy is ever reached; existing slash commands and the "Unknown command" fallback are both unaffected (regression); Sandy handler exceptions are caught gracefully; source-text check confirms the EOD digest block is positioned after `DTA-MARKET-BENCHMARK-001` and before the class's `# Helpers` section; `_sandy_loop()` respects `self._running` and never raises | N/A |
| `sandy/sandy_mcp_server.py` | Self-Learning Ecosystem Phase 7c: NEW — VS Code chat/MCP surface for Sandy. A local stdio MCP (Model Context Protocol) server exposing 3 read-only tools (`ask_sandy(query)`, `sandy_daily_digest()`, `sandy_health_snapshot()`) that delegate directly to `SandySupervisor`'s already-tested Phase 7a methods — zero new logic, zero mutation. Uses `mcp` package's v2 `MCPServer` API (`mcp.server.mcpserver.MCPServer`, not the renamed-in-v2 `FastMCP`). Verified starts cleanly as a stdio subprocess with no crash | No |
| `.vscode/mcp.json` | Phase 7c: NEW — registers the `sandy` MCP server for VS Code chat (`type: stdio`, launches `sandy/sandy_mcp_server.py` via the repo's own `.venv` interpreter). Committed to source control (not gitignored) so the whole team shares the same wiring, per VS Code's own documented convention | N/A |
| `requirements-dev.txt` | Phase 7c: added `mcp>=1.0.0` under a new "VS Code chat/MCP surface" section — deliberately NOT added to `requirements.txt` (the production Docker image never needs this; it is a local dev-only tool) | No |
| `tests/test_sandy_mcp_server.py` | Phase 7c: NEW — 6/6 tests (T01–T06): all 3 tool functions correctly delegate to `SandySupervisor`'s existing methods with the exact arguments/return values; exactly 3 tools are registered on the server; source-text safety scan confirms zero `execution_engine`/`order_manager`/`dhan_feed`/`broker`/`risk_control` imports; `.vscode/mcp.json` is valid JSON and correctly wired to `sandy_mcp_server.py` | N/A |
| `predictive_gap/pga_idr_bridge.py` | Post-ecosystem roadmap item 1 (root-cause fix): NEW — replaces `pga_learning.py`'s broken Category B call, `IDRRepository.add_observation()`, a method that never existed. Audit confirmed `IDRRepository` (`market_learning/idr_repository.py`) is a feature/pattern-keyed DNA store with NO symbol column anywhere in its schema (cannot record a per-symbol observation), and `HistoricalBehaviourEngine`'s `OutcomeRecord` requires many fields PGA's Category B payload doesn't have (fabricating them would inject zero-filled noise into a live, trading-consequential evidence pool). This bridge is an isolated, append-only, advisory-only observation log (`data/pga/idr_reinforcement_observations.jsonl`) — `record_reinforcement_observation()`, `get_observation_count()`, `get_last_observations()` — never read back into any live trading decision | No |
| `predictive_gap/pga_learning.py` | `_try_reinforce_idr()` now calls `pga_idr_bridge.record_reinforcement_observation()` instead of the non-existent `IDRRepository.add_observation()` — Category B actions now actually succeed instead of always silently failing | No |
| `tests/test_pga_idr_bridge.py` | NEW — 8/8 tests (T01–T07 + T02b): real JSONL append, count with/without symbol filter, last-n oldest-first, fail-open on I/O error, `_try_reinforce_idr()` now succeeds, fail-open on bridge exception, source-text safety scan confirms zero `IDRRepository`/`idr_repository` import | N/A |
| `kde/kde_idr_evidence_bridge.py` | Post-ecosystem roadmap item 4 (revised same day per explicit user direction — decisions must be made autonomously from computed evidence, not by an admin naming discovery/dna_id pairs): auto-promotion into the real, live-consequential IDR store is now FULLY AUTOMATIC, gated on computed reproducibility — a (scheme_id, dna_id) pattern must be independently re-observed across ≥2 separate KDE runs, each with a genuinely DIFFERENT `years_used` set (proves new underlying data, not a re-run on identical input), non-degrading score (≤0.05 drop tolerance), and `score.overall≥0.60` on every observation. When met, `evaluate_discoveries_for_idr_evidence()` itself calls `IDRRepository.add_evidence()` — no human names a pair. Each pattern merges at most once ever (bounds total irreversible writes); evidence stays bounded (confidence/effect_size ≤0.30). `request_live_merge(scheme_id, dna_id)` remains only as a manual override/escape hatch, not the primary path, and is still never invoked automatically anywhere. Verified against real production HKAP/KDE data: currently 0 proposals generate (today's discoveries are sector-rotation/clustering-based with no `dna_ids` populated and scores <0.60) — confirms the gate is correctly, safely dormant until genuinely qualifying DNA-level evidence exists | No |
| `kde/__init__.py` | Added `kde_idr_evidence_bridge` re-exports (`evaluate_discoveries_for_idr_evidence`, `get_pending_proposals`, `get_merge_history`, `request_live_merge`) | No |
| `hkap/hkap_kde_bridge.py` | Post-ecosystem roadmap item 4: `_run_impl()` now also calls `evaluate_discoveries_for_idr_evidence(result.discoveries, years_used=sorted(packages.keys()))` (try/except-wrapped, non-fatal) immediately after `KDEEngine.run()` — passes the run's real years_used so the bridge can detect genuinely new data across re-runs | No |
| `tests/test_kde_idr_evidence_bridge.py` | NEW — 14/14 tests (T01–T14): score/dna_ids gating, first observation stays SHADOW (no premature merge), identical years_used never counts as confirmation, new years_used + non-degrading score auto-merges with zero human step, degrading score blocks merge, bounded evidence even at score=1.0, ACTIVE never merges twice (bounds total writes), pending-proposal filtering/active-exclusion, merge ledger reflects `merged_by="auto:kde_idr_evidence_bridge"`, manual override still works as an escape hatch, unknown pair never touches the repository, fail-open on garbage input and repository exception, source-text scan confirms `request_live_merge` is never called automatically anywhere. Full `test_idr_repository.py` standalone suite re-run clean: 90/90 pass (unmodified, confirms IDR itself untouched) | N/A |
| `decision_tracer/dtrace_scheduler.py` | NEW — Post-roadmap Priority 4 (decision_tracer activation): automates DTA-001's existing, unchanged `run_dta()` across a bounded sample (`MAX_SYMBOLS_PER_DAY=10`) of symbols that had a real decision recorded that day in `control_tower.db`'s `ct_decisions`/`ct_cycles` tables (previously required manual CLI invocation per symbol). `run_daily_trace_batch()`, `get_last_run_summary()`, `get_run_history()`. Diagnostic/observability only — output never read back into any live decision. Fail-open throughout | N/A |
| `decision_tracer/__init__.py` | Exported the 3 new `dtrace_scheduler` functions; updated module STATUS docstring from DISCONNECTED to PARTIALLY ACTIVE | No |
| `orchestrator/master_orchestrator.py` | Priority 4: added one non-fatal, try/except-wrapped EOD call to `run_daily_trace_batch()`, matching the established EOD-stage convention | No |
| `sandy/sandy_supervisor.py` | Priority 4: added `_poll_dtrace_scheduler()` as a 14th poller | No |
| `tests/test_dtrace_scheduler.py` | NEW — 7/7 tests (T01–T07): zero decisions → zero symbols traced without ever calling `run_dta()`; distinct symbols deduped and bounded by `max_symbols`; one symbol's trace failure never blocks the rest; run-history read-back oldest-first; fail-open on a corrupt/non-sqlite db file; fail-open when `run_dta()` always raises; source-text safety scan. Verified against real local `control_tower.db`: correctly reports 0 symbols traced (no decisions recorded locally today) | N/A |
| `meta_learning/regime_map_evidence_log.py` | NEW — Post-roadmap Priority 5 part 1 (MetaStrategyController's static `_REGIME_MAP` activation, ACQUISITION): isolated, append-only per-(regime,strategy) trade-outcome observer (`data/meta_learning/regime_map_evidence.jsonl`) — additive alongside, never replacing, the existing live `RegimeStrategyMap.record()` call (which only persists running aggregates, not the individual time-ordered records a proper train/OOS statistical validation requires). `record_regime_trade()`, `get_records()`, `get_pairs_with_evidence()` | N/A |
| `strategy_lab/regime_map_refinement_engine.py` | NEW — Priority 5 part 2 (SYNTHESIS+GOVERNANCE): evidence-gated, demotion-only mechanism for `_REGIME_MAP`'s hand-curated candidate lists — mirrors the exact shadow-then-live lifecycle (`WAITING_FOR_EVIDENCE→SHADOW_ACTIVE→ACTIVE/REJECTED`, `ACTIVE→ROLLED_BACK`) already proven by KDA-CRE-001/RSL-001/debate-weight-refinement. `MIN_SAMPLE_FOR_VALIDATION=30` per (regime,strategy) pair, honoring `strategy_governance_roadmap.md`'s own human-authored Phase 2 trigger ("30-50 clean live trades") as a floor for full automation. Time-ordered 70/30 split, bootstrap CI on OOS win-rate vs 0.50 null, sign consistency, minimum effect magnitude. Never promotes a strategy not already hand-curated for a regime, never empties a regime's candidate list (safety floor), never imports/modifies `regime_strategy_map.py`. `get_effective_regime_map()` (mtime-cached, falls back to the raw static map on any error), `run_daily_refinement_check()`, `get_refinement_status()`, `get_ledger_history()` | N/A |
| `strategy_lab/meta_strategy_controller.py` | Priority 5: added `_effective_regime_map()` accessor (mirrors `_effective_constants()`/`get_effective_weights()` precedent); `get_active_strategies()` and `print_activation_report()` now read through it instead of the bare `_REGIME_MAP` module dict. Verified byte-identical behavior today (confirmed 0 active demotions in a fresh environment) | No |
| `orchestrator/master_orchestrator.py` | Priority 5: added `record_regime_trade()` call alongside the existing `regime_strategy_map.record()` call (same loop, additive only); added one non-fatal, try/except-wrapped EOD call to `regime_map_refinement_engine.run_daily_refinement_check()` | No |
| `sandy/sandy_supervisor.py` | Priority 5: added `_poll_regime_map_refinement()` as a 15th poller | No |
| `tests/test_regime_map_evidence_log.py` | NEW — 7/7 tests (T01–T07): record shape, regime/strategy filtering, time-ordering, distinct-pairs accessor, fail-open on I/O error, fail-open/skip on corrupt JSONL lines, safety-contract source scan | N/A |
| `tests/test_regime_map_refinement_engine.py` | NEW — 14/14 tests (T01–T14): insufficient sample waits, strong losing signal starts shadow, winning signal never shadowed (demotion-only scope proven), shadow blocked until enough new evidence, shadow promotes/rejects correctly on reconfirming/contradicting evidence, ACTIVE auto-rolls-back on recovery, effective map matches static map with zero demotions, an active demotion removes exactly one (regime,strategy) pair, safety floor refuses to empty a regime, only pre-existing candidates are ever evaluated, cooldown blocks immediate re-check, fail-open, safety-contract source scan (confirms `regime_strategy_map.py` is never imported) | N/A |
| `tests/test_meta_strategy_controller_regime_map_bridge.py` | NEW — 3/3 tests: effective map == static map with no demotions, `get_active_strategies()` behavior unchanged (regression), fails open to the static map if the refinement engine raises | N/A |
| `tests/test_sandy_supervisor.py` | Updated poller-count assertions from 13→14→15 across both Priority 4 and Priority 5 additions | N/A |
| `.gitignore` | Added `data/dta/` (Priority 4) and `data/meta_learning/` (Priority 5) | No |
| `check_phase_c_ready.py`, `check_phase_d_ready.py` | Root-cause fix: Windows consoles default to a legacy codepage (cp1252) that cannot encode these scripts' box-drawing/check-mark characters, crashing with `UnicodeEncodeError` on every real run — reproduced and confirmed. Added `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` at import time (falls back to '?' instead of crashing on truly unrenderable glyphs) — cosmetic-only fix, zero readiness-gate logic touched. `check_phase_d_ready.py` also had a real, separate bug in its informational "Bonus: Phase C Health Summary" block — referenced a non-existent `opportunities.first_seen_at` column (real column is `created_at`, confirmed via live schema inspection); fixed. Verified against the REAL production `market_behavior.db` on the VPS (via a temporary, read-only `docker cp`+`docker exec` — no container restart, no code deploy needed for this diagnostic run): Phase C is genuinely NOT READY (2/5 gates pass — signal_births=165 and archetype frequencies OK; sector_conviction/theme_phase_history/lifecycle-diversity all fail, all 40 real opportunities are stuck in `DISCOVERED`, none have ever transitioned to `ACTIVE`/`WATCHING`/terminal). Phase D is NOT READY (0/5 gates). Per `oios_build_status.md`'s own explicit rule ("Phase C may not start until all 5 gates pass on real live data"), Phase D/E engines (velocity_engine, transition_model, outcome_distributor, cause_intelligence, propagation_engine, adaptive_intelligence, shadow_scorer) were deliberately NOT built or wired this phase — would fabricate readiness the real data does not support | No |
| `orchestrator/master_orchestrator.py` | ROOT-CAUSE FIX (found via the C-Ready-5/Phase-D investigation above): `oios/engine/ele.py::run_ele_daily()` — the Edge Lifecycle Engine that computes RE score + conviction and drives the OIOS `DISCOVERED→ACTIVE/WATCHING/INVALID` state machine — was fully built, tested (its own standalone Phase A0/B/C/D/E test suites, 204/205 passing pre-existing) but **never invoked anywhere in the live pipeline**. `master_orchestrator.py` only ever called the Layer 1A/1B scanners (which create `signal_births`/`opportunities` rows) and `signal_outcome_tracker` (price-outcome measurement only) — nothing computed `conviction_score` for any opportunity, so every one stayed at its default `0.0`, permanently below `ACTIVE_THRESHOLD=6.0`. This was the actual root blocker for OIOS C-Ready-5 (100% DISCOVERED concentration) and all 5 Phase D gates (all downstream of state transitions that could never fire). Added one non-fatal, try/except-wrapped call to `run_ele_daily()` immediately after the existing Layer 1A/1B signal-scan block (same 16:45 IST post-market slot, `_run_post_market_scan`), gated by a small JSON state file (`data/oios/ele_age_advance_state.json`) that advances each live opportunity's `age_trading_days` by exactly 1 once per real trading day before calling `run_ele_daily()` (per that function's own documented contract: "does NOT advance age_trading_days — that is the caller's responsibility") — prevents double-advancing age if this method is ever re-triggered same day. Shadow-safe: reads/writes only `market_behavior.db`'s OIOS tables; `oios/engine/ele.py`, `oios/domain/state_machine.py`, and the OIOS execution bridge (one-directional, observes real trades only) were not modified — this only activates an already-built, already-isolated research pipeline, never touches `DecisionEngine`/`CapitalRiskEngine`/`OrderManager`. Verified against a local read-only copy of the real production `market_behavior.db`: before fix, 40/40 opportunities `DISCOVERED` (conviction=0.0 for all); after running the newly-wired code path, 39/40 correctly transitioned to `ACTIVE` (conviction=10.0, capped, driven by real confirming signal evidence that had never been scored before) and 1/40 correctly remained `DISCOVERED` (insufficient evidence) — proves the fix is real and the engine's own scoring logic (untouched) behaves exactly as its existing test suite already established | No |
| `tests/oios/` | Full existing 205-test OIOS suite re-run clean after the wiring change: 204/205 pass — 1 pre-existing, unrelated failure (`test_pe06_get_events_for_symbol`, a date-drift test bug hardcoding 2026-06-14/15 against a `days_back=60` window from the real current date, in `oios/engine/event_ingestion.py` — confirmed unrelated to `ele.py`/state-machine, not touched, not fixed, flagged only) | N/A |
| `trade_monitoring/strategy_regime_health_engine.py` | NEW — Self-learning module #24 (StrategyHealthMonitor regime-aware disabling, per `strategy_governance_roadmap.md`'s own Phase 2 design). Reuses `meta_learning/regime_map_evidence_log.py`'s existing per-(regime,strategy) trade evidence (Priority 5, additive) instead of duplicating tracking. Same shadow-then-live lifecycle (`WAITING_FOR_EVIDENCE→SHADOW_ACTIVE→ACTIVE/REJECTED`, `ACTIVE→ROLLED_BACK`) as `regime_map_refinement_engine.py`. `MIN_SAMPLE_FOR_VALIDATION=30`. Disables a strategy ONLY in the specific regime real evidence shows it losing in — SHM's global EARLY_ABORT/win-rate/drawdown/Sharpe gates completely untouched. `get_regime_disabled_strategies(regime)` | No |
| `orchestrator/master_orchestrator.py` | #24 wiring: `_run_strategy_lab()`'s `excluded_strategies` now additionally unions in `get_regime_disabled_strategies(snapshot.regime.value)`; one non-fatal EOD refinement-check call | No |
| `sandy/sandy_supervisor.py` | #24: added `_poll_shm_regime_health()` as a 16th poller | No |
| `tests/test_strategy_regime_health_engine.py` | NEW — 11/11 tests: WAITING/SHADOW/ACTIVE/ROLLED_BACK lifecycle, demotion-only scope (winning signal never shadowed), `get_regime_disabled_strategies()` scoping, cooldown, fail-open, safety-contract source scan (confirms `strategy_health_monitor.py` never imported) | N/A |
| `data_feeds/data_integrity_tracker.py` | Self-learning module #26 prerequisite: added `get_all_tracked_symbols_today()` — small, additive, read-only accessor (union of corruption/sanity/refresh symbol keys). `DataIntegrityTracker`'s own reset-at-midnight behavior completely untouched | No |
| `data_feeds/trust_score_history.py` | NEW — #26 part 1 (ACQUISITION): fixes postponed_improvements.md's own stated architectural prerequisite ("Persist DataIntegrityTracker state to disk... otherwise trust scores are meaningless over multi-day horizon"). `record_daily_trust_snapshot()` persists a daily snapshot of every tracked symbol's trust score to `data/data_integrity/trust_score_history.jsonl` (gitignored), guarded to run at most once/calendar-date. `get_multi_day_trust_score(symbol, window_days=5)`, `get_history_days_count()` | N/A |
| `opportunity_engine/trust_weighted_ranking_engine.py` | NEW — #26 part 2 (SYNTHESIS+GOVERNANCE): `get_effective_score(symbol, raw_score, today_trust_score)` — implements postponed_improvements.md item #12's proposed `trust_adjusted_score = score * (TRUST_WEIGHT * data_trust_score + (1-TRUST_WEIGHT))`, `TRUST_WEIGHT=0.15`. Gated on a DATA-READINESS check (not a P&L bootstrap test — trust score is an engineering integrity signal, not an outcome hypothesis, so the categorically correct gate is "enough persisted history exists", `MIN_HISTORY_DAYS=5`) globally AND per-symbol; falls back to the raw, unadjusted score byte-identically below either gate. Degenerate-symbol guard: a symbol with zero-variance low scores across the whole window is treated as neutral (restart-artifact protection, per postponed_improvements.md's own stated caution) | No |
| `opportunity_engine/equity_scanner_ai.py` | #26 wiring: the existing sector-momentum re-rank sort (`prepared.sort(key=lambda c: (_sector_rank(c), -float(c.get("score",0))))`) now sorts by `get_effective_score(...)` instead of the bare `score` — falls back to the exact original ranking whenever the data-readiness gate is not met (today: always, 0 persisted history days) | No |
| `orchestrator/master_orchestrator.py` | #26 wiring: one non-fatal EOD call to `run_daily_snapshot_and_check()` | No |
| `sandy/sandy_supervisor.py` | #26: added `_poll_trust_weighted_ranking()` as a 17th poller | No |
| `tests/test_trust_score_history.py` | NEW — 8/8 tests: snapshot persistence, same-day-retrigger no-op, multi-day averaging with correct gating, fail-open, `get_all_tracked_symbols_today()` union accessor, safety-contract source scan | N/A |
| `tests/test_trust_weighted_ranking_engine.py` | NEW — 9/9 tests: global/per-symbol gate fallback to raw score, gated adjustment reduces/leaves-unchanged score correctly, degenerate-flat-score guard, fail-open, daily-check gate reporting, safety-contract source scan | N/A |
| `trade_monitoring/strategy_health_monitor.py` | Self-learning module #25 prerequisite: added 4 small, additive public read-only accessors — `get_profile_suggestion()` (wraps the existing, unchanged `_compute_profile_suggestion()`), `get_trades_count()`, `get_tracked_strategy_names()`, `get_profile_type()` — plus one new public setter `apply_profile_override(strategy_name, profile, reason)` that writes ONLY the pre-existing, metadata-only `strategy_profile_type` field (never touches `disabled_reason`/`trades`/`total_r`/any governance field) | No |
| `trade_monitoring/strategy_profile_refinement_engine.py` | NEW — Self-learning module #25 (profile-aware governance auto-apply). Builds the GOVERNANCE layer explicitly left out of the original suggestion heuristic ("Manual assignment always overrides auto-suggestion; requires explicit confirmation to apply"). `MIN_SAMPLE_FOR_AUTO_APPLY=50` (roadmap's own stricter Phase 3 trigger, above the 30-trade suggestion floor). Only ever auto-applies a HIGH-confidence suggestion after shadow-reconfirmation on `MIN_SHADOW_NEW_TRADES=15` NEW trades; self-correcting (a later, different HIGH-confidence, reconfirmed suggestion re-shadows and can supersede an already-applied one) | No |
| `orchestrator/master_orchestrator.py` | #25 wiring: one non-fatal EOD call to `run_daily_refinement_check(self.strategy_health)` | No |
| `sandy/sandy_supervisor.py` | #25: added `_poll_shm_profile_refinement()` as an 18th poller | No |
| `tests/test_strategy_profile_refinement_engine.py` | NEW — 11/11 tests: below-sample/no-suggestion/non-HIGH-confidence all stay WAITING, HIGH-confidence divergence starts SHADOW, matching-current-profile is a no-op, shadow blocked until enough new trades, shadow promotes and calls `apply_profile_override()` exactly once with the correct args, shadow rejects on divergent reconfirmation, cooldown, fail-open, safety-contract source scan | N/A |
| `learning_system/sizing_bounds_refinement_engine.py` | NEW — Self-learning module #27 (confidence+strategy-history combined sizing — calibration layer). AUDIT FINDING: both halves of the originally-proposed "combined sizing" formula are ALREADY LIVE as two independent, already evidence-gated factors (confidence-based `risk_pct` scaling since 2026-05-25; `perf_weight` bounded [0.5x,2.0x] gated on `MIN_SAMPLE=10` official trades, already self-learning) — rebuilding a third formula would be redundant. The real gap: nobody validates whether `perf_weight`'s own [0.5x,2.0x] bounds are well-calibrated against real outcomes. This module does exactly that — splits closed trades into "boosted" (perf_weight>1.05) vs "not boosted" cohorts, validates via the same time-ordered 70/30 bootstrap-CI scorecard used elsewhere this session whether the boosted cohort's win-rate is reliably higher, and evidence-widens/narrows the UPPER bound only (bounded ±0.30), shadow-then-live. **Deliberately placed in `learning_system/`, not `risk_control/`** — `sandy/sandy_supervisor.py` has its own explicit safety-contract test forbidding any `risk_control` import anywhere in that package; this location lets Sandy poll it for observability without violating that boundary | No |
| `risk_control/portfolio_allocation_ai.py` | #27 wiring: after `get_performance_weight()` is read (non-KDA-authoritative path only, unchanged), one additional re-clamp line through `get_effective_perf_weight_bounds()` — byte-identical behavior (falls back to the original hardcoded (0.5,2.0)) until real evidence validates an adjustment | No |
| `orchestrator/master_orchestrator.py` | #27 wiring: `record_sizing_outcome()` call added right after `perf_tracker.record_trade()` in the same EOD "Performance Evaluation" loop (uses the current `get_performance_weight(strategy)` value as a documented proxy for "was this trade boosted"); one non-fatal EOD refinement-check call | No |
| `sandy/sandy_supervisor.py` | #27: added `_poll_sizing_bounds_refinement()` as a 19th (20th overall) poller | No |
| `tests/test_sizing_bounds_refinement_engine.py` | NEW — 12/12 tests: insufficient-sample waits, strong cohort-difference signal starts shadow with correct direction, no-difference stays waiting, shadow blocked/promotes/rejects correctly, ACTIVE auto-rolls-back on degradation, default bounds unchanged with no adjustment, an active adjustment widens the upper bound bounded by `MAX_BOUND_ADJUSTMENT`, cooldown, fail-open, safety-contract source scan (confirms `portfolio_allocation_ai.py` never imported) | N/A |
| `tests/test_sandy_supervisor.py` | Poller-count assertions bumped 15→16→17→18→19 (`_poll_shm_regime_health`, `_poll_trust_weighted_ranking`, `_poll_shm_profile_refinement`, `_poll_sizing_bounds_refinement`) | N/A |
| `.gitignore` | Added `data/trade_monitoring/` (modules #24 and #25's isolated stores), `data/data_integrity/` (module #26's trust score history), `data/risk_control/` (module #27's sizing calibration evidence — confirmed via `git ls-files` that none of these 3 paths had any previously-tracked files) | No |
| `analysis/capital_reserve_readiness_engine.py` | NEW — Self-learning module #28 (Intelligent Capital Reserve readiness gate, postponed_improvements.md item #5 "Best-Trade Priority Queue"). AUDIT FINDING: item #5's own original author flagged it "Complexity: Medium-high — requires position replacement logic. Discuss architecture before implementing" — unlike #24-27, an actual capital-reserve swap would CLOSE an already-open real position, categorically more execution-consequential. Built ONLY the safe half: a fully automatic, evidence-gated READINESS SIGNAL (never a live swap mechanism), mirroring `knowledge_system/equity_hedge_shadow_engine.py`'s own `READY_FOR_LIVE` precedent ("marks the authentication decision only, not a live-trading switch"). Reuses 100% existing evidence — `analysis/rejection_attribution_monitor.py` (Self-Learning Ecosystem Phase 4, already live) already resolves every `MAX_POSITIONS_CAP` rejection via real T+1/T+3/T+5 price follow-through and classifies each as CORRECT_REJECTION/FALSE_REJECTION/NEUTRAL — a `FALSE_REJECTION` on a capital-cap rejection is direct evidence of real opportunity cost from capital constraints. `MIN_SAMPLES_FOR_READINESS=30`, `FALSE_NEGATIVE_READY_THRESHOLD=40%` — below either, reports `WAITING_FOR_EVIDENCE`/`NOT_YET_WARRANTED`; only reports `READY_FOR_DESIGN` (never executes anything) once real evidence justifies the "discuss architecture" investment. Placed in `analysis/` (not `risk_control/`) so `sandy/sandy_supervisor.py`'s own safety-contract test (forbids `risk_control` imports anywhere in that package) is respected | No |
| `orchestrator/master_orchestrator.py` | #28 wiring: one non-fatal EOD call to `run_daily_readiness_check()` — read-only status check only, never touches any open position | No |
| `sandy/sandy_supervisor.py` | #28: added `_poll_capital_reserve_readiness()` as a 21st poller | No |
| `tests/test_capital_reserve_readiness_engine.py` | NEW — 7/7 tests: below-sample-floor waits, low/high false-negative-rate correctly produce NOT_YET_WARRANTED/READY_FOR_DESIGN, ledger logs only on status transition (not repeated identical status), fail-open on both accessor and daily-check, safety-contract source scan | N/A |
| `tests/test_sandy_supervisor.py` | Poller-count assertions bumped 19→20→21 for `_poll_capital_reserve_readiness` | N/A |
| `.github/workflows/runtime-state-guard.yml` | User Q1 (VPS/local deploy-safety, addressed in advance): extended PATTERNS with broad-safe globs for the 12 confirmed-git-clean self-learning data directories created this session (`data/trade_monitoring/`, `data/risk_control/`, `data/data_integrity/`, `data/meta_learning/`, `data/oios/`, `data/data_feeds/`, `data/debate/`, `data/kda_cre/`, `data/hkap/`, `data/kde/`, `data/sandy/`, `data/rejection_attribution/`) plus narrower subdirectory-only patterns (`data/dta/scheduler/*`, `data/ars/scheduler/*`, `data/ars/sd/*`) for new subdirectories under otherwise-legacy-tracked parents (`data/ars/`, `data/dta/` themselves are NOT globbed broadly — they contain pre-existing, deliberately-tracked historical report snapshots that a blanket glob would false-positive on). Verified via a standalone `bash -n` syntax check plus a real `git ls-files` dry-run against the new patterns: zero unexpected matches (only the 2 already-excluded `data/frz/backups/...` legacy archive files matched, same as before this change) | No |
| `learning_system/capital_risk_facade.py` | NEW — User Q2 (Sandy agent-performance coverage): tiny read-only facade re-exporting `risk_control.capital_risk_engine.get_last_cycle_dominant_rejection_reason()` so `sandy/` can surface it without importing `risk_control` directly (would violate `sandy_supervisor.py`'s own existing `test_T09_no_forbidden_imports` safety contract — same constraint that already forced module #27 out of `risk_control/` earlier this session) | No |
| `sandy/sandy_supervisor.py` | User Q2: added 2 new pollers — `_poll_debate_agents` (raw per-debater accuracy via `debate_vote_tracker.get_debater_accuracy()`, distinct from the existing refinement-engine-status poller) and `_poll_capital_risk_engine` (via the new facade above), both `BASELINE_LOOP` category. Added `_lookup_roster_agent()` (lazy-imports `orchestrator.master_orchestrator.ALL_AGENTS`, substring match) and `_coverage_summary()` (dynamic `N/total` coverage count, never hardcoded). `_answer_impl`'s `"sandy status <name>"` branch now falls back to the roster lookup before saying "I don't recognise X" — an agent with no dedicated poller now gets an honest "runs deterministically, no dedicated poller yet" reply instead of a false "unrecognised" response. Added `"sandy agents"`/`"sandy coverage"` command. Total registered pollers: 21→23 | No |
| `tests/test_sandy_supervisor.py` | User Q2: refactored T01/T02's poller-patching from one 23-deep chained `with a, b, c, ...:` statement (which started hitting Python's `SyntaxError: too many statically nested blocks` once the 2 new pollers were added) to a `contextlib.ExitStack`-based helper (`_patch_all_pollers()`) — same effective behavior, no nesting-depth limit. Added T11 (roster-aware fallback acknowledges an `ALL_AGENTS` member with no dedicated poller), T12 (`"sandy agents"` reports dynamic `N/total` coverage), T13a-f (both new pollers wrap their accessors correctly; facade re-exports the real `risk_control` getter byte-for-byte; regression-guards that `sandy_supervisor.py` still contains zero `risk_control` import strings even with the new poller). 22/22 pass | N/A |
| `strategy_lab/strategy_generator_ai.py` | Rejection-attribution audit follow-up: added `classify_rejection_reason()` — a pure, unit-testable helper mirroring `_assign()`'s exact gate order, giving the 2 previously-generic-"ASSIGN_REJECTED" gates (bear-market equity BUY rejection, volatile-regime low-confidence rejection) their own distinct labels (`BEAR_MARKET_BUY_REJECTED`, `VOLATILE_LOW_CONFIDENCE_REJECTED`). Zero change to `_assign()` itself or which signals get dropped — pure evidence-labeling addition, reusing the already-live `rejection_tracker.py`/`rejection_attribution_monitor.py` pipeline (real T+1/T+3/T+5 price follow-through) with zero further wiring needed | No |
| `orchestrator/master_orchestrator.py` | Rejection-attribution audit follow-up: `[StrategyLabReject]` block's inline reason-classification if/elif chain replaced with a call to the new `classify_rejection_reason()` (extract-method refactor for testability — same 3 pre-existing labels, `RR_x.x_below_min_y.y` / `STRATEGY_DISABLED` / `ASSIGN_REJECTED`, unchanged; only the 2 new labels are additive). `_regime_match` hoisted out of the per-signal loop to `_cycle_regime` (computed once per cycle, was being recomputed identically on every iteration) | No |
| `tests/test_strategy_lab_rejection_attribution.py` | NEW — 8/8 tests (T01–T08): bear-market-BUY / RR-below-min / volatile-low-confidence / disabled / generic-fallback all correctly attributed in `_assign()`'s own gate order; bear-market SELL and volatile-high-confidence correctly NOT mis-flagged; regression guard proving real `str`-mixin `SignalType`/`SignalDirection` Enum members compare correctly via `==` without needing `str()` (which would silently break the match, e.g. `str(SignalType.EQUITY) == "SignalType.EQUITY"`, not `"equity"` — verified empirically on this repo's Python 3.14). Full adjacent regression (`test_equity_strategylab_observation_only_001.py`, `test_arch_005_integration.py`, `test_dta_attribution_trail_001.py`, `test_rejection_attribution_monitor.py`) re-run clean: 63/63 pass | N/A |
| `strategy_lab/strategy_generator_ai.py` | User-directed follow-up (same day): converted the 2 gates just labeled above from hard StrategyLab rejects into KDA-only observations — bear-market equity BUY and volatile-regime low-confidence signals are no longer dropped (`return None`); they now get `strategy_health_status` set (`BEAR_MARKET_BUY_OBSERVED` / `VOLATILE_LOW_CONFIDENCE_OBSERVED`) and are still forwarded, exactly mirroring the existing DTA-EQUITY-STRATEGYLAB-OBSERVATION-001 pattern already used for SHM/PerfTracker/MetaController judgement. The volatile-regime reduced-sizing risk margin (qty halved) is still applied even when confidence is low — that stays a safety measure, not a gate. The RR-below-minimum gate was deliberately left untouched (risk-hygiene rule, not a trading judgement — same protected category as `RiskGuardian`). Net effect: KDA (Layer 10) is now the sole decision-maker for these 2 cases too; downstream risk gates (CapitalRiskEngine, RiskControl, RiskGuardian, MultiAgentDebate) are unchanged and still independently apply | No |
| `tests/test_equity_strategylab_observation_only_001.py` | Updated T005 (bear-market equity BUY: now asserts forwarded + `BEAR_MARKET_BUY_OBSERVED`, not `None`) and added T010 (volatile-regime low-confidence: asserts forwarded + `VOLATILE_LOW_CONFIDENCE_OBSERVED` + sizing still halved). RR-below-minimum (T006) left unchanged/still-enforced. Confirmed the 5 unrelated `test_arch_001_integration.py` failures seen in one combined pytest run are pre-existing test-file cross-contamination (a `utils` import-cache corruption from running specific test files together in one session) — reproduced as absent when that file is run alone, both before and after this change; not caused by and out of scope for this edit | N/A |
| `execution_engine/brokers/dhan_broker.py` | DH-901 ROOT-CAUSE FIX: added `DhanBroker.reload_token()` — previously the only broker adapter with NO way to hot-swap its Dhan access token; it loaded the token once at construction and never again. Confirmed live via production logs/DB: 2 fully-approved trades today (COALINDIA, SBIN) and 2 yesterday (COALINDIA, TATACONSUM) died at order placement with Dhan error DH-901 `Invalid_Authentication: Client ID or user generated access token is invalid or expired` — while the data feed (separately refreshed) kept working fine all day. Root cause: Dhan tokens are DAILY SESSION tokens; the daily refresh automation (DTA-001 cron @ 01:50 IST + DTA-002 in-process sync) only ever called `feed_manager.dhan.reload_token()`, never anything on `OrderManager._broker`/`OptionsOrderManager._broker` — so any process running across a token-rotation boundary (i.e. not restarted since the last refresh) silently kept placing orders with a stale, invalid token | No |
| `execution_engine/order_manager.py`, `execution_engine/options_order_manager.py` | DH-901 fix: added `reload_broker_token(new_token)` to both — delegates to `self._broker.reload_token()` when the broker supports it (checked via `hasattr`, matching this file's own established broker-capability-check convention used for `get_order_status`/`place_sl_order`/`get_fill_details`); returns `False` (never raises) in paper mode or for brokers without reload support (Zerodha/AngelOne adapters unaffected — untouched) | No |
| `scripts/dhan_auth/dhan_token_sync.py` | DH-901 fix: `maybe_sync()`/`_do_sync()` gained an optional `order_managers: Optional[list] = None` parameter (fully backward-compatible — omitting it is byte-identical to prior behaviour). When provided, after the existing feed-reload succeeds, calls `reload_broker_token(new_token)` on each order manager, best-effort (one failing/raising broker never blocks the others or the feed's own already-successful reload); result dict gained a `broker_reloads: Dict[str, bool]` field. Added a module-level logger (previously had none) | No |
| `orchestrator/master_orchestrator.py` | DH-901 fix: `_sync_dhan_token()`'s call to `maybe_sync()` now passes `order_managers=[self.order_manager, self.options_order_manager]` — the only call site in production, so both live brokers now get the fresh token every 5 minutes alongside the data feed, closing the gap with zero new schedule/cadence | No |
| `tests/test_dhan_token_sync_002.py` | DH-901 fix: NEW T222–T231 (10 tests): backward-compatible default (no `order_managers` arg unchanged), every passed order manager receives the new token, one broker's failure/exception never blocks the others or the feed's own success, no reload attempted on `NO_CHANGE`, `OrderManager.reload_broker_token()` correctly delegates to/fails-open around the broker, `DhanBroker.reload_token()` actually swaps `access_token` and reconnects (the real fix), and rejects an empty token without mutating state. Full existing DTA-002 suite (T201–T221) + adjacent `test_dta_attribution_trail_001.py`/`test_dta_system_011.py` re-run clean: 50 + 78 = 128 pass | N/A |
| `scripts/final_trading_architecture_shadow_001.py` | DTA-SHADOW-BACKFILL-001 root-cause fix (found while auditing the "20/20 + 5/5 vs real movers" comparison, 4 days of real production data): `run_shadow_day()` only ever runs SAME-DAY in production (no `trade_date` arg from the orchestrator) — T+1 opening prices structurally cannot exist yet on the same day, so `compute_c2_score()` always got `opening=None` and `select_c2_top_n()` (which only ranks candidates with a non-`None` c2_score) silently selected **0 stocks in both directions, every single day, forever** — confirmed live (`c2_up_selected=0, c2_down_selected=0, t1_data_available=False`) and reproduced against the real replay DB. Added `run_shadow_day_backfill()` — retroactively re-processes the previous trading date (whose T+1 = today, now available) with `force=True`, so the real top-5-per-direction C2 selection can finally compute. Idempotent/no-op once a date already has a genuine selection (`c2_up_selected>0` or `c2_down_selected>0`) — never re-processes an already-finalized date. Also added `_previous_ohlcv_trade_date()` and `_latest_summary_for_date()` helpers | No |
| `orchestrator/master_orchestrator.py` | DTA-SHADOW-BACKFILL-001: added one non-fatal EOD call to `run_shadow_day_backfill()` immediately after the existing `run_shadow_day()` call, same try/except convention. Also wired `reclassify_stale_benchmarks_silent()` (below) right after the existing `run_daily_market_benchmark_silent()` call | No |
| `tests/test_final_trading_architecture_shadow_001.py` | DTA-SHADOW-BACKFILL-001: NEW T078–T088 (11 tests): previous-date lookup, latest-summary lookup (picks most recent by timestamp), skip when no previous date / already finalized, forced re-run when stuck at 0/0 or never-run, graceful `db_not_found` handling, safety-contract (`no_trades_generated`/`no_broker_calls` preserved), and a REAL replay-DB integration test (T088) that reproduces the exact live bug (same-day run selects 0/0) then proves the backfill call correctly targets and would re-process the previous date. Full suite re-run clean: 88/88 pass | N/A |
| `opportunity_engine/market_opportunity_benchmark.py` | DTA-MARKET-BENCHMARK-RECLASSIFY-001 (same audit): the daily benchmark's same-day comparison structurally can't see shadow-evidence data for its own day either (same timing root cause as above) — confirmed on real data: 100% of 315 real movers across 4 audited days landed in only categories A/B, telling us nothing about actual 20-pool/5-selection quality. Added `reclassify_stale_benchmarks()` — re-checks the last 5 days' `IN_UNIVERSE_NOT_IN_20POOL` movers against the now-more-complete shadow evidence (thanks to the backfill fix above) and appends a corrected classification to a **separate** `MARKET_BENCHMARK_RECLASSIFIED.jsonl` file whenever it genuinely improves (e.g. into C/D/E/F) — the original per-day files are never mutated (audit trail preserved). Upgraded misses are also fed into the existing `rejection_audit.db` evidence store (`quality_tier="MARKET_BENCHMARK_MISS"`), same mechanism as the original same-day feed | No |
| `tests/test_dta_market_benchmark_001.py` | DTA-MARKET-BENCHMARK-RECLASSIFY-001: NEW 7 tests: upgrades a stale B-category record once matching shadow evidence exists, original file never mutated (byte-for-byte comparison), no-op when shadow data still absent, non-B-category rows ignored, upgraded misses correctly feed `rejection_tracker`, never raises on total failure, EOD wiring present and try/except-contained. Full suite re-run clean: 28/28 pass | N/A |
| `scripts/knowledge_system/selection_characteristic_analyzer_001.py` | Selection-quality improvement plan, item 2 (DOWN-direction research gap): added `high_rsi_and_high_mom_accel` to `COMBINATIONS` — the OPPOSITE RSI extreme from the one already-validated UP finding (`low_rsi_and_high_mom_accel`), same acceleration signal. For DOWN this is a genuine reversal/breakdown-from-overbought thesis, not a mechanical mirror of the other 4 UP-oriented combos (which the existing direction-flip banding already tests symmetrically for DOWN with no positive signal found). Purely additive — automatically picked up by Phase 7's existing daily discovery loop for both directions, zero further wiring. Verified live: with fresh post-backfill-fix data (as_of=2026-09-14), confirmed the ORIGINAL DOWN combos still show negative lift across the board — the gap is real, not a data-staleness artifact | No |
| `scripts/knowledge_system/fingerprint_discovery_001.py` | Updated a stale docstring ("5 pre-specified combinations x both directions (10 pairs)" → generic "every pre-specified combination x both directions") to stay accurate now that a 6th combo exists — no logic change, `run_discovery_silent()` already iterated the live `COMBINATIONS` list, never a hardcoded count | No |
| `tests/test_phase7_fingerprint_discovery_001.py` | NEW 4 tests: new combo registered + no duplicate names, rule correctly matches high-RSI+high-accel and rejects other band combos, `run_discovery_silent()` automatically evaluates it for both UP and DOWN with zero extra wiring, confirmed NOT in `STATICALLY_REGISTERED` (remains eligible for normal automatic promotion). Full selection-intelligence regression (phase2e/3/4/5/6/7/8/9): 152/152 pass | N/A |
| `predictive_gap/liquid_universe_builder_001.py` | Selection-quality plan item 3 (user-confirmed target: ~500 symbols, ADV-filtered): NEW — root-cause fix for the live trading universe being a STATIC embedded 230-symbol list (`_run_weekly_universe_rebuild()`'s own comment: "NSE direct is unreachable from the VPS... the embedded 230-symbol list is used"). Builds a liquidity-ranked universe from Dhan's already-accessible security master (~2,438 real NSE EQ symbols via `broad_market_universe.py`) + real ADV computed from `ohlcv_daily`. Fails safe (`None`) below a 50%-of-target sanity floor. **Live-verified finding (real VPS data): `ohlcv_daily` currently only has historical price data for ~209 symbols (a subset of the existing 230-symbol universe) — the broader ~2,438 Dhan symbols have NEVER had OHLCV collected, so ranking can only surface 208 real candidates today, correctly triggering the safe fallback to the embedded 230 list.** The real prerequisite to reach 500 is expanding daily OHLCV *collection* itself to the broader Dhan symbol set — a separate, larger infrastructure task, flagged to the user, not yet started. Symbol-suffix normalization fix applied during live testing: `ohlcv_daily` stores symbols WITH `.NS` suffix while `broad_market_universe.py` returns bare symbols — `_compute_adv_crore()` now strips the suffix for correct matching | No |
| `opportunity_engine/market_scanner.py` | Selection-quality plan item 3: `_write_universe_json()` now tries `build_liquid_universe()` first, falling back to the original embedded 230-symbol list (`_builtin_universe()`) on any failure/sanity-floor-abort — the fallback path is byte-identical to prior behaviour, so today's actual deployed behaviour is unchanged (confirmed live: sanity floor aborts, embedded 230 is used) until OHLCV collection is widened | No |
| `orchestrator/master_orchestrator.py` | Selection-quality plan item 3: `_run_weekly_universe_rebuild()`'s docstring/log line updated to describe the new liquid-universe-first, embedded-fallback behavior — no logic change in this file itself (the try/except/success-check around `_write_universe_json()` is untouched) | No |
| `tests/test_liquid_universe_builder_001.py` | NEW — 15/15 tests: real ADV computation from a synthetic `ohlcv_daily` (index-symbol exclusion, lookback-window correctness, fail-open on missing DB), ranking + target-size + output-schema correctness, sector-reuse-from-embedded-230 vs `UNKNOWN` fallback, sanity-floor abort, fail-open on any exception, `_write_universe_json()` wiring (liquid-universe-used, embedded-fallback-on-None, embedded-fallback-on-exception) verified via `__file__` monkeypatching to redirect writes to `tmp_path`. Full regression (this file + `test_dta_market_benchmark_001.py` + `test_final_trading_architecture_shadow_001.py`): 131/131 pass | N/A |
| `system_monitor/system_monitor.py` | Item #6 (OE latency, CONFIRMED already resolved, no code change): `LAYER_LATENCY_CRIT_OVERRIDES["OpportunityEngine"]=90_000` (90s, vs the blanket 5s default) already exists specifically because "yfinance NSE batch may be slow; don't abort up to 90s" — combined with the already-built background async price/RSI refresh threads in `equity_scanner_ai.py`, this fully resolves the original 2026-04-29 complaint (2.7-5.15s cycles hitting CRITICAL and aborting). Current baseline: Full cycle 172ms HEALTHY. Verified only, zero lines changed | N/A |
| `orchestrator/master_orchestrator.py` | Item #8 (cosmetic label fix): `_run_post_restore_governance()`'s Telegram line changed `"Live prices: {len(_live_pf)}/{len(_syms)}"` → `"Live symbols: {len(_live_pf)}/{len(_syms)}"` — the underlying calculation already used deduplicated symbols (a prior fix), this was purely the remaining label-wording ambiguity called out in postponed_improvements.md item #8 | No |
| `execution_engine/order_manager.py` | Item #9 (exposure accounting audit): performed the 3-layer audit (`PortfolioAllocationAI._MAX_SINGLE_TRADE_FRACTION`/`_MAX_SYMBOL_NOTIONAL_FRACTION`=15%/15%, `OrderManager.MAX_CAPITAL_PER_TRADE_PCT`=15% — all consistent, confirmed already kept in sync via existing code comments). Found one real, previously-undocumented drift risk: `config.MAX_POSITIONS` (dynamic, capital-tier-scaled: 3/5/8) and `OrderManager.MAX_OPEN_POSITIONS` (static `15`) represent the same "max concurrent positions" concept but were NOT mechanically linked — `OrderManager`'s guard currently never fires in practice only because `CapitalRiskEngine`'s tighter, earlier-pipeline cap always binds first; if `config.MAX_POSITIONS` were ever raised above 15 for a larger capital tier without this file being updated, `OrderManager`'s "last-resort" guard would silently become STRICTER than the primary gate it's meant to sit behind. Fixed: `MAX_OPEN_POSITIONS = max(15, config.MAX_POSITIONS + 5)` — verified zero behavior change today (still resolves to exactly `15`), now drift-proof going forward | No |
| `data_feeds/data_feed_manager.py` | Item #10 prerequisite: added `get_options_chain_state_snapshot(symbol)` — small, additive, read-only accessor over the existing `_options_chain_state` dict (source/fetched_at/is_live/chain). Never triggers a new chain fetch | No |
| `data_feeds/options_health_history.py` | NEW — Item #10 (Options Health Metrics Dashboard, postponed_improvements.md, flagged 2026-05-15 "meaningful now that options are LIVE"). Honors the item's own explicit prerequisite ("At least 1 week of stable LIVE options data") via `MIN_HISTORY_DAYS=7` — `get_options_health_status(symbol)` reports `WAITING_FOR_EVIDENCE` below that, never fabricating a "normal" baseline from too little data. `record_daily_options_health_snapshot()` persists strike completeness, OI coverage %, chain freshness, and PCR plausibility (0.5-2.0 per the item's own stated expected range) per tracked symbol (NIFTY/BANKNIFTY) to `data/data_feeds/options_health_history.jsonl` (gitignored), guarded to run once/date. Reuses the real, already-fetched chain state — never triggers a new live options fetch | N/A |
| `notifications/telegram_bot.py` | Item #10 wiring: `/status` command gained a gated `Options Health:` line — shows real computed averages once the 7-day gate is met, otherwise shows `"gathering evidence (N/7 days)"` — never fabricates readiness | No |
| `orchestrator/master_orchestrator.py` | Item #10 wiring: one non-fatal EOD call to `record_daily_options_health_snapshot()` | No |
| `sandy/sandy_supervisor.py` | Item #10: added `_poll_options_health()` as a 22nd poller | No |
| `tests/test_options_health_history.py` | NEW — 8/8 tests: per-symbol snapshot persistence, same-day-retrigger no-op, below-floor waits, gated status computes real averages, no-chain-state symbol skipped, fail-open on snapshot and status accessor, safety-contract source scan | N/A |
| `tests/test_sandy_supervisor.py` | Poller-count assertions bumped 21→22 for `_poll_options_health` | N/A |
| `scripts/seed_broad_ohlcv_history_001.py` | DTA-UNIVERSE-EXPANSION-001 phase 2 (OHLCV data-collection widening, user-authorized): NEW — standalone, one-time/re-runnable seeder. Deliberately NOT wired into `master_orchestrator.py` or any live cycle — reuses `oios/data/ohlcv_fetcher.py::run_daily_fetch()` (the exact function the existing live daily OIOS refresh already calls) directly with an explicit broad symbol list (`predictive_gap/broad_market_universe.py::load_broad_nse_equity_symbols()`, ~2,438 symbols), bypassing `universe_stocks` entirely so the one-time bulk historical backfill (90-day lookback) never touches the live cycle's timing budget. Run manually via `docker exec` on the VPS (~24 min real runtime): `ok=2206 failed=232 rows_inserted=135784`. Coverage: `ohlcv_daily` distinct symbols went from 210 → 2,416 (2,412 with ≥20 bars, enough for the 20-day ADV lookback `liquid_universe_builder_001.py` needs). The 232 "failures" are almost all `possibly delisted; no price data found (1d <today> -> <today>)` for symbols ALREADY up to date through yesterday (today's still-forming intraday bar simply isn't available yet from yfinance) — not real data gaps; a couple (`VASCONEQ`, `VISASTEEL`) are genuine curl-timeout/no-timezone failures, logged and skipped, non-fatal | No |
| `data/market_behavior.db` (`universe_stocks` table, production data only — no code change) | DTA-UNIVERSE-EXPANSION-001 phase 2: after the backfill, re-ran `build_liquid_universe()` — now returns a real, ADV-ranked 500-symbol result (mix of existing NIFTY500 names and genuinely new discoveries: ETERNAL, IFCI, NETWEB, GROWW, etc.), confirming the sanity-floor gate built in phase 1 now clears. Inserted those 500 symbols into `universe_stocks` (`INSERT ... ON CONFLICT DO UPDATE SET is_active=1 WHERE is_active=0`, never touching already-active rows) via a one-time ad-hoc `docker exec` script (not committed — pure data-seeding, same table the existing daily OIOS refresh already reads via `SELECT symbol FROM universe_stocks WHERE is_active=1`, zero new wiring needed). `universe_stocks` active count: 230 → 573 (343 genuinely new symbols; 157 of the top-500 already overlapped the original 230). Going forward, the already-existing, already-scheduled daily OIOS refresh job now automatically maintains all 573 with 1-day incremental fetches (safe/bounded — full history already seeded, no more 90-day backfills needed on the live path) | No |
| `data/nifty500_universe.json` (production data only — no code change) | DTA-UNIVERSE-EXPANSION-001 phase 2: manually triggered `opportunity_engine/market_scanner.py::_write_universe_json()` (the same function the scheduled `_run_weekly_universe_rebuild()` calls) post-backfill — confirmed live: `[UniverseWriter] Wrote 500 symbols to /app/data/nifty500_universe.json (source=liquid_broad_market)`. This is the first time the live scanner's universe file has ever been genuinely liquidity-ranked from the broad ~2,438-symbol Dhan universe rather than falling back to the embedded 230 | No |
| `data/market_behavior.db` (`universe_stocks` table) | DTA-UNIVERSE-EXPANSION-001 phase 2, follow-up: `universe_stocks` active count raised 230→573 (343 new symbols from the liquidity-ranked top-500) so the pre-existing daily OIOS refresh (`SELECT symbol FROM universe_stocks WHERE is_active=1`) maintains all of them going forward. Confirmed structurally: `mover_discovery_v3_shadow_runner.py::_load_universe_symbols()` and `scripts/final_trading_architecture_shadow_001.py::_load_universe()` BOTH already read this exact same table — the V3 20-UP/20-DOWN pool and C2 top-5/5 selection automatically benefit from the larger universe on their next run, zero further code change needed | No |
| `learning_system/learning_observation_ledger.py`, `opportunity_engine/klp_outcome_engine.py`, `analysis/rejection_attribution_monitor.py` | DTA-EOD-BACKLOG-CAP-001 (root-cause fix, found during a full audit of the Selection Intelligence Layer's "is it really self-improving" question): `data/eod_status.json` on the VPS showed the EOD learning pipeline stuck at `STARTED` (never `COMPLETED`) for at least the last several days — silently blocking EVERY downstream self-learning stage (Phase 2E-9, KSL-001, RSL-001, market benchmark reclassify, etc.) despite all being correctly wired and previously verified only via manual, one-off invocations. Root-caused via isolated stage timing directly on production: AMLS/MLC complete in ~30s (not the culprit), but `LOL.fill_pending_outcomes()` took 55.6s processing 322 items with 254 STILL pending, and `KLP-002.fill_pending_outcomes()` was still mid-way through an unbounded, one-yfinance-call-per-pending-item backlog when killed at a 280s test timeout. None of these 3 stages had ever had a bound on how many backlog items they'd process in one call — once behind (from any single missed/interrupted cycle, e.g. a deploy landing mid-cycle), the backlog only grows, making each subsequent day's run take longer, increasing the odds of the NEXT interruption — a snowballing, self-perpetuating stall with no single "hung" line of code. Added an additive `max_items=200` cap (never raises, never drops data — items beyond the cap simply stay pending for the next run) to `LOL.fill_pending_outcomes/_fill_outcomes_impl`, `KLPOutcomeEngine.fill_pending_outcomes/_fill_impl` (both its main + SCAN_NO_SETUP sub-loops share one fetch budget), and `RejectionAttributionMonitor.run_daily_cycle/_run_impl`. Verified on production post-deploy: all 3 now complete in bounded time (0.5s / 70.2s / 17.6s) instead of open-ended growth — unblocks the entire downstream self-learning chain going forward | No |
| `.gitignore` | DTA-EOD-BACKLOG-CAP-001: added `data/eod_status.json` and `data/klp/` (confirmed via `git ls-files` never previously tracked — pure runtime artifacts) | No |

---

**Every code modification must be followed by a full deploy cycle. No exceptions.**
Local, VPS, and container must always run identical code.

### Steps (run in order, do not skip)

```powershell
# 1 — Commit all changed files
git add <files>
git commit -m "<message>"

# 2 — Push to origin
git push origin main

# 3 — Deploy to VPS (single command)
# safe_pull.sh backs up runtime data → pulls → restores data → no knowledge lost
ssh -i ~/.ssh/trading_vps root@178.18.252.24 "cd /root/ai-trading-brain && bash scripts/safe_pull.sh && python3 scripts/generate_build_manifest.py && docker compose build --no-cache && docker compose down && docker compose up -d && sleep 8 && docker compose ps"
```

### Definition of done
The deploy is complete **only** when `docker compose ps` shows **both** containers `Up … (healthy)`:
```
ai-trading-brain          Up N seconds (healthy)
trading-dashboard         Up N seconds (healthy)
```

If either container is not healthy — **stop, diagnose with `docker logs ai-trading-brain`, fix, redeploy before continuing.**

### Why
- The `data/` volume is persistent (`./data:/app/data`) — runtime files survive restarts
- `docker compose build --no-cache` ensures the new Python source is baked in, not cached
- A partial deploy (local committed, VPS not updated) is a split-brain state and must never persist

---

## Protected Modules (edit only with explicit instruction)

These modules are stable and load-bearing. They may evolve, but only when the
user explicitly asks. Never modify them speculatively or as a side-effect of
another change.

| Module | Why protected | What explicit approval unlocks |
|---|---|---|
| `risk_guardian/risk_guardian.py` | Kill-switch logic is intentional — wrong edit = real money loss | New kill conditions, threshold tuning |
| `strategy_lab/backtesting_ai.py` | WFT/OOS quality gates are calibrated | New metrics, additional test types |
| `validation_engine/` | 6-stage pipeline, promotion criteria set | New validation stage, adjusted thresholds |
| `strategy_lab/evolved_strategies/` | Earned through evolution runs — not hand-written | Parameter tuning, fitness re-evaluation |
| `data/` directory | Live SQLite databases + persisted state | Schema migrations only, never destructive |
| `data_feeds/dhan_feed.py` | Broker auth + order routing — bugs here affect live orders | New endpoint mapping, fallback logic |
